# 心理守护 — 本地启动指南

包含各端启动命令、端口分配、测试账号。所有命令在项目根目录 `mental-chat/` 下执行。

---

## 一、首次准备

```bash
# 后端 venv + 依赖 + 迁移
cd backend && python3 -m venv venv && source venv/bin/activate \
  && pip install -r requirements.txt && pip install httpx \
  && DJANGO_DEBUG=true python manage.py migrate && deactivate && cd ..

# AI 服务 venv + 依赖
cd ai_service && python3 -m venv venv && source venv/bin/activate \
  && pip install -r requirements.txt && deactivate && cd ..

# 前端依赖（pnpm workspace 一次装齐）
pnpm install
```

---

## 二、必备环境变量

| 变量 | 设到哪 | 值 |
|------|--------|------|
| `DJANGO_DEBUG` | 启动 backend 时 | `true`（不带就要求生产 SECRET_KEY、强制连 Postgres） |
| `AI_API_KEY` | 启动 ai_service 时 | DashScope/通义千问 API key（`sk-...`） |

`.env` 文件里有 `AI_API_KEY=sk-5efb1bd489c9428aa767164820c11008`，可直接复用。

---

## 三、启动各端（每条单开一个终端）

### 1. Django 后端

```bash
cd backend
source venv/bin/activate
DJANGO_DEBUG=true python manage.py runserver 0.0.0.0:8000
```

启动后：
- REST API：http://localhost:8000/api/
- Swagger 文档：http://localhost:8000/api/docs/
- WebSocket：ws://localhost:8000/ws/alerts/
- 健康检查：http://localhost:8000/health

### 2. Flask AI 服务（通义千问）

```bash
cd ai_service
source venv/bin/activate
AI_API_KEY=sk-5efb1bd489c9428aa767164820c11008 python app.py
```

启动后：
- 普通回复：POST http://localhost:5000/respond
- **流式回复**（SSE）：POST http://localhost:5000/respond_stream
- 危机检测：POST http://localhost:5000/crisis_check
- TTS：POST http://localhost:5000/tts

### 3. 管理端（React，admin-web）

```bash
cd admin-web
pnpm dev
```

启动后：http://localhost:5173/admin-web/

### 4. 应急端（Vue，emergency-web）

```bash
cd emergency-web
pnpm dev --port 5174
```

启动后：http://localhost:5174/emergency/

> 必须显式 `--port 5174`，不然会和管理端的 5173 冲突。

### 5. 患者端（Expo / React Native）

**浏览器调试模式（推荐用来验流式对话）：**
```bash
cd patient-app
pnpm web
```
启动后：http://localhost:8081/

**手机扫码模式：**
```bash
cd patient-app
pnpm start
```
然后用 Expo Go App 扫终端的二维码，手机和电脑在同一 Wi-Fi 下。

> 浏览器模式下原生 API（语音识别、通讯录、原生录音）不可用，但聊天、情绪、登录等核心功能都能跑。

### 6. 支持者小程序（Taro，可选）

```bash
cd support-miniapp
pnpm dev:weapp
```

编译后用微信开发者工具打开 `support-miniapp/dist` 目录。

---

## 四、端口一览

| 服务 | 端口 | URL |
|------|------|------|
| Django 后端 | **8000** | http://localhost:8000 |
| Flask AI 服务 | **5000** | http://localhost:5000 |
| 管理端 | **5173** | http://localhost:5173/admin-web/ |
| 应急端 | **5174** | http://localhost:5174/emergency/ |
| 患者端 (Expo Web) | **8081** | http://localhost:8081/ |

---

## 五、测试账号

| 角色 | 用户名 | 密码 | 进哪个端 |
|------|--------|------|---------|
| 患者 | `patient1` | `Patient123!` | 患者端 |
| 专业人员（医生）| `doctor1` | `Pro123456!` | 应急端 |
| 家属/陪伴者 | `supporter1` | `Support123!` | 患者端（守护者视角） |
| 管理员 | `testadmin` | `Admin123456!` | 管理端 |

> 这些是本地 SQLite 中已有用户。如果重置了数据库（删 `backend/db.sqlite3`），用 `python manage.py shell` 重建：
> ```python
> from api.models import User
> User.objects.create_user("patient1", email="patient1@test.com", password="Patient123!", role="patient")
> User.objects.create_user("doctor1", email="doctor1@test.com", password="Pro123456!", role="professional")
> User.objects.create_user("supporter1", email="supporter1@test.com", password="Support123!", role="supporter")
> User.objects.create_superuser("testadmin", email="admin@test.com", password="Admin123456!", role="admin")
> ```

---

## 六、本地数据存储

| 类型 | 位置 |
|------|------|
| 数据库 | SQLite `backend/db.sqlite3` |
| Channel layer | 内存（重启丢 WebSocket 连接，不影响数据库） |
| Cache | 内存（local-memory） |
| Celery | **未启动**（不影响主流程，crisis check 走异步 HTTP 兜底） |

要切到 PostgreSQL + Redis：在 backend 启动时设置 `DB_*` 和 `REDIS_URL` 环境变量即可。

---

## 七、常见问题

| 问题 | 解决 |
|------|------|
| 后端启 1 秒就退出，报 `DJANGO_SECRET_KEY required` | 命令前加 `DJANGO_DEBUG=true` |
| AI 服务启动报 `AI_API_KEY required` | 命令前加 `AI_API_KEY=...` |
| 管理端跑起来但调 API 跨域 | 后端必须用 `DJANGO_DEBUG=true`，会自动 `CORS_ALLOW_ALL_ORIGINS` |
| 应急端打不开 / 端口冲突 | 5173 被管理端占了，应急端必须 `--port 5174` |
| 患者端 Expo Web 登录后白屏 | 浏览器强刷 ⌘+Shift+R；旧的 Ionicons bug 已用 emoji tab 图标修掉 |
| AI 对话感觉一次性出字、不流式 | 已修复——后端首字节 89ms，纯流式输出 |

---

## 八、停止服务

各终端 `Ctrl+C` 即可。如果端口残留进程：

```bash
# 查
lsof -i :8000 -i :5000 -i :5173 -i :5174 -i :8081 -P -n | grep LISTEN
# 杀
kill <PID>
```
