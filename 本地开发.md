# 本地开发指南

## 1. 项目组成

当前仓库包含以下可运行模块：

- `backend`：Django / DRF / Channels 后端。
- `ai_service`：Flask AI 微服务。
- `patient-app`：Expo / React Native 患者端，同时包含守护者视角。
- `admin-web`：React / Vite 管理后台。
- `emergency-web`：Vue 3 / Vite 紧急响应看板。
- `packages/shared`：前端共享 API Client、类型和文章分类。


## 2. 必备环境

| 工具 | 建议版本 | 说明 |
| --- | --- | --- |
| Node.js | >= 18 | 前端开发 |
| pnpm | >= 9 | workspace 包管理 |
| Python | >= 3.9 | 后端和 AI 服务 |
| Redis | 可选 | Celery、缓存、Channels；本地没有 Redis 时部分能力有兜底 |
| Expo Go / 浏览器 | 可选 | 调试患者端 |

## 3. 安装依赖

```bash
# 根目录安装前端依赖
pnpm install

# 后端依赖
cd backend
python -m venv venv
# Windows
.\venv\Scripts\activate
# macOS/Linux
# source venv/bin/activate
pip install -r requirements.txt
DJANGO_DEBUG=true python manage.py migrate

# AI 服务依赖
cd ../ai_service
python -m venv venv
# Windows
.\venv\Scripts\activate
# macOS/Linux
# source venv/bin/activate
pip install -r requirements.txt
```

## 4. 环境变量

本地开发最少需要：

```bash
DJANGO_DEBUG=true
AI_API_KEY=<your_qwen_or_dashscope_key>
AI_SERVICE_URL=http://localhost:5000/respond
```

说明：

- `DJANGO_DEBUG=true`：使用 SQLite、本地开发密钥、宽松 CORS。
- `AI_API_KEY`：AI 微服务必填；不设置会启动失败。
- `AI_SERVICE_URL`：Django 调用 AI 微服务的同步接口地址，默认是 `http://localhost:5000/respond`。

## 5. 启动服务

建议分多个终端启动：

```bash
# 终端 1：Django 后端，http://localhost:8000
DJANGO_DEBUG=true pnpm dev:backend

# 终端 2：Flask AI 服务，http://localhost:5000
AI_API_KEY=<your_key> pnpm dev:ai

# 终端 3：管理后台，http://localhost:5173/admin-web/
pnpm dev:admin

# 终端 4：紧急响应端，http://localhost:5174/emergency/
pnpm dev:emergency

# 终端 5：患者端/守护者端，Expo
pnpm dev:patient
# 或 Web 调试
cd patient-app && pnpm web
```

> 根目录脚本中保留了 `dev:miniapp`，但当前仓库没有对应目录，不能作为本项目当前可运行端使用。

## 6. 常用地址

| 服务 | 地址 |
| --- | --- |
| Django API | `http://localhost:8000` |
| Flask AI | `http://localhost:5000` |
| 管理后台 | `http://localhost:5173/admin-web/` |
| 紧急响应端 | `http://localhost:5174/emergency/` |
| 患者端 Web | `http://localhost:8081`，以 Expo 输出为准 |
| 告警 WebSocket | `ws://localhost:8000/ws/alerts/?token=<jwt_access>`，仅 `admin` / `professional` |
| 患者-守护者聊天 WebSocket | `ws://localhost:8000/ws/chat/<peer_id>/?token=<jwt_access>` |

## 7. 测试账号建议

可通过注册接口或 Django shell 创建：

| 角色 | 示例账号 | 说明 |
| --- | --- | --- |
| 患者 | `patient1` | 使用 `patient-app` 患者视角 |
| 守护者 | `supporter1` | 使用 `patient-app` 守护者视角 |
| 专业人员 | `doctor1` | 使用 `admin-web` 和 `emergency-web` |
| 管理员 | `admin1` | 使用 `admin-web` 用户和分配管理 |

示例：

```bash
cd backend
DJANGO_DEBUG=true python manage.py shell
```

```python
from api.models import User, PatientAssignment, SupporterLink
patient = User.objects.create_user('patient1', password='Patient123!', role='patient')
doctor = User.objects.create_user('doctor1', password='Doctor123!', role='professional')
supporter = User.objects.create_user('supporter1', password='Support123!', role='supporter')
admin = User.objects.create_user('admin1', password='Admin123!', role='admin', is_staff=True, is_superuser=True)
PatientAssignment.objects.create(patient=patient, professional=doctor)
SupporterLink.objects.create(patient=patient, supporter=supporter)
```

## 8. 当前已实现的开发链路

- 患者记录情绪：`patient-app` -> `POST /api/mood_entries`。
- 患者量表评估：情绪记录 Tab 内进入评估模块 -> `POST /api/assessments`。
- AI 流式对话：`patient-app` -> `POST /api/chat/send_message_stream` -> Django 代理 -> Flask `/respond_stream`。
- 危机检测：发送 AI 消息后调用 Flask `/crisis_check`，命中关键词则创建 `CrisisAlert`。
- SOS：患者端 AI 对话页点击 SOS，创建高风险告警；后端强制绑定当前登录患者，不能伪造其他用户。
- 告警推送：Django signal -> Channels -> `/ws/alerts/?token=...`，连接时校验 JWT 和 `admin` / `professional` 角色，前端轮询兜底。
- 守护者私聊：`/ws/chat/<peer_id>/?token=...`，断开时使用 HTTP 兜底。
- 科普文章：后台维护文章，患者端按分类查看；支持外部 URL 跳转。


## 10. 验证命令

```bash
# 患者端类型检查
pnpm -C patient-app exec tsc --noEmit

# 后端配置检查
cd backend
DJANGO_DEBUG=true python manage.py check

# Django 测试入口
DJANGO_DEBUG=true python manage.py test
```
