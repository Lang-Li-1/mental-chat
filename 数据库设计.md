# 心理守护 — 数据库设计文档

> 最后更新：2026-03-17（含架构评审与第二轮优化）

---

## 一、数据库概述

### 1.1 数据库选型

| 项目 | 说明 |
|------|------|
| 数据库引擎 | SQLite 3 |
| ORM 框架 | Django 4.2 ORM |
| 认证扩展 | djangorestframework-simplejwt 5.3+ |
| 主键策略 | BigAutoField（64 位自增整数） |
| 时区配置 | Asia/Shanghai |
| 字符编码 | UTF-8 |

### 1.2 数据库表总览

系统共包含 **21 张数据表**，按用途分为三类：

| 分类 | 表数量 | 说明 |
|------|--------|------|
| 业务数据表 | 12 张 | 系统核心业务实体 |
| Django 框架表 | 6 张 | 认证、权限、会话、迁移等框架支撑 |
| 关联关系表 | 3 张 | 多对多关系的中间表 |

**全部表清单：**

| 序号 | 表名 | 分类 | 说明 |
|------|------|------|------|
| 1 | `users` | 业务 | 用户表（自定义，继承 AbstractUser） |
| 2 | `mood_entries` | 业务 | 情绪记录表 |
| 3 | `chat_messages` | 业务 | 对话消息表 |
| 4 | `chat_feedbacks` | 业务 | 对话反馈表 |
| 5 | `crisis_alerts` | 业务 | 危机告警表 |
| 6 | `assessment_results` | 业务 | 心理评估结果表 |
| 7 | `patient_assignments` | 业务 | 患者-专业人员分配表 |
| 8 | `supporter_links` | 业务 | 守护者-患者关联表 |
| 9 | `encouragement_messages` | 业务 | 鼓励消息表 |
| 10 | `recovery_tasks` | 业务 | 康复任务表 |
| 11 | `recovery_badges` | 业务 | 成就徽章表 |
| 12 | `articles` | 业务 | 科普文章表 |
| 13 | `auth_group` | 框架 | 用户组表 |
| 14 | `auth_permission` | 框架 | 权限定义表 |
| 15 | `auth_group_permissions` | 框架 | 用户组-权限关联表 |
| 16 | `django_admin_log` | 框架 | 管理后台操作日志表 |
| 17 | `django_content_type` | 框架 | 模型内容类型注册表 |
| 18 | `django_migrations` | 框架 | 数据库迁移记录表 |
| 19 | `django_session` | 框架 | 会话存储表 |
| 20 | `users_groups` | 关联 | 用户-用户组多对多关联表 |
| 21 | `users_user_permissions` | 关联 | 用户-权限多对多关联表 |

---

## 二、实体关系图（ER 图）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Entity-Relationship Diagram                       │
│                                                                             │
│  ┌──────────┐                                                               │
│  │  users   │──1:N──→ mood_entries                                          │
│  │          │──1:N──→ chat_messages ──1:1──→ chat_feedbacks                  │
│  │  (PK: id)│──1:N──→ crisis_alerts                                         │
│  │          │──1:N──→ assessment_results                                     │
│  │          │──1:N──→ recovery_tasks                                         │
│  │          │──1:N──→ recovery_badges                                        │
│  │          │                                                                │
│  │          │──1:N──→ encouragement_messages (sender)                        │
│  │          │←─N:1──  encouragement_messages (receiver)                      │
│  │          │                                                                │
│  │          │──M:N──→ patient_assignments ←──M:N── users (professional)      │
│  │          │──M:N──→ supporter_links     ←──M:N── users (supporter)         │
│  │          │                                                                │
│  │          │──1:N──→ articles (author, 可为空)                               │
│  │          │──1:N──→ crisis_alerts.handled_by (处理人, 可为空)                │
│  └──────────┘                                                                │
│                                                                             │
│  独立关系:                                                                    │
│  chat_messages ──1:1──→ chat_feedbacks (message_id UNIQUE)                   │
│  patient_assignments: UNIQUE(patient_id, professional_id)                   │
│  supporter_links: UNIQUE(supporter_id, patient_id)                          │
│  recovery_badges: UNIQUE(user_id, badge_type)                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、业务数据表详细设计

### 3.1 users（用户表）

用户表是系统的核心实体，继承 Django AbstractUser 模型，扩展了角色（role）、手机号（phone）等字段。系统通过 `role` 字段区分四种用户角色：患者（patient）、专业人员（professional）、守护者（supporter）、管理员（admin）。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| username | VARCHAR(150) | UNIQUE, NOT NULL | — | 用户名 |
| password | VARCHAR(128) | NOT NULL | — | 哈希密码（Django PBKDF2） |
| email | VARCHAR(254) | UNIQUE, NULLABLE | None | 邮箱地址（NULL 规避空字符串唯一冲突） |
| phone | VARCHAR(20) | UNIQUE, NULLABLE | None | 手机号码 |
| role | VARCHAR(20) | NOT NULL | "patient" | 用户角色 |
| first_name | VARCHAR(150) | NOT NULL | "" | 名（继承字段） |
| last_name | VARCHAR(150) | NOT NULL | "" | 姓（继承字段） |
| is_active | BOOLEAN | NOT NULL | True | 是否激活 |
| is_staff | BOOLEAN | NOT NULL | False | 是否可访问管理后台 |
| is_superuser | BOOLEAN | NOT NULL | False | 是否超级管理员 |
| last_login | DATETIME | NULLABLE | NULL | 最后登录时间 |
| date_joined | DATETIME | NOT NULL | 自动生成 | Django 注册时间 |
| created_at | DATETIME | NOT NULL | 自动生成 | 自定义创建时间 |

**role 字段枚举值：**

| 值 | 含义 | 系统权限 |
|----|------|---------|
| patient | 患者（天使） | 情绪记录、AI 对话、心理评估、康复计划、紧急求助 |
| professional | 专业人员 | 查看分配患者数据、处理危机告警、内容管理 |
| supporter | 守护者（亲友） | 关联患者、查看脱敏状态、发送鼓励消息 |
| admin | 管理员 | 全部数据访问、用户管理、分配管理、数据导出 |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| sqlite_autoindex_users_1 | username | UNIQUE |
| sqlite_autoindex_users_2 | email | UNIQUE |
| sqlite_autoindex_users_3 | phone | UNIQUE |

---

### 3.2 mood_entries（情绪记录表）

记录患者每次的情绪评分和文字描述，是情绪追踪和趋势分析的数据基础。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| user_id | BIGINT | FK → users.id, NOT NULL | — | 所属用户 |
| mood_score | INTEGER | NOT NULL, CHECK(1≤v≤10) | — | 情绪评分（1-10），含数据库级验证器 |
| content | TEXT | NOT NULL | "" | 文字描述（可为空字符串） |
| created_at | DATETIME | NOT NULL | 自动生成 | 记录时间 |

**mood_score 分值含义：**

| 分值范围 | 对应表情 | 含义 |
|---------|---------|------|
| 1-2 | 非常低落 | 情绪极差 |
| 3-4 | 比较低落 | 情绪较差 |
| 5-6 | 一般 | 情绪平稳 |
| 7-8 | 比较开心 | 情绪较好 |
| 9-10 | 非常开心 | 情绪极佳 |

**验证器：** MinValueValidator(1), MaxValueValidator(10) — 数据库级约束，防止无效分值写入

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| mood_entries_user_id_b5eec78b | user_id | 普通索引 |
| idx_mood_user_created | (user, created_at) | 复合索引（优化情绪趋势查询） |

**排序规则：** `ORDER BY created_at DESC`

**外键约束：** user_id → users.id，级联删除（CASCADE）

---

### 3.3 chat_messages（对话消息表）

统一存储用户消息和 AI 回复，通过 `is_ai_response` 区分消息来源，通过 `session_id` 将消息分组为不同对话会话。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| user_id | BIGINT | FK → users.id, NOT NULL | — | 所属用户 |
| content | TEXT | NOT NULL | — | 消息内容 |
| is_ai_response | BOOLEAN | NOT NULL | False | 是否为 AI 回复 |
| session_id | CHAR(32) | NOT NULL, INDEX | uuid4 | 会话 ID（UUID 格式） |
| created_at | DATETIME | NOT NULL | 自动生成 | 消息时间 |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| chat_messages_user_id_cf3eadd8 | user_id | 普通索引 |
| chat_messages_session_id_c5afc568 | session_id | 普通索引（显式 db_index=True） |
| idx_chat_user_session | (user, session_id, created_at) | 复合索引（优化会话消息查询） |

**排序规则：** `ORDER BY created_at ASC`（同一会话内按时间正序）

**外键约束：** user_id → users.id，级联删除（CASCADE）

**设计说明：**
- `session_id` 使用 UUID v4 生成，前端首次发起对话时生成新 UUID，后续消息沿用同一 session_id
- 每次 AI 对话请求携带最近 20 条同 session 历史消息作为上下文
- is_ai_response=False 为用户消息，is_ai_response=True 为 AI 回复

---

### 3.4 chat_feedbacks（对话反馈表）

记录用户对 AI 回复消息的评价（点赞/点踩），用于 AI 对话质量评估。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| message_id | BIGINT | FK → chat_messages.id, UNIQUE, NOT NULL | — | 关联消息（一对一） |
| user_id | BIGINT | FK → users.id, NOT NULL | — | 评价用户 |
| is_positive | BOOLEAN | NOT NULL | — | 是否正面评价 |
| created_at | DATETIME | NOT NULL | 自动生成 | 评价时间 |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| sqlite_autoindex_chat_feedbacks_1 | message_id | UNIQUE |
| chat_feedbacks_user_id_af3f6b18 | user_id | 普通索引 |

**外键约束：**
- message_id → chat_messages.id，级联删除（CASCADE）
- user_id → users.id，级联删除（CASCADE）

**设计说明：** 与 chat_messages 为一对一关系（OneToOneField），每条 AI 消息最多一条反馈记录。

---

### 3.5 crisis_alerts（危机告警表）

危机告警的完整生命周期管理，记录从创建到确认到解决的全过程。支持两种创建方式：AI 对话中自动检测创建、患者主动 SOS 求助创建。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| user_id | BIGINT | FK → users.id, NOT NULL | — | 告警用户 |
| level | VARCHAR(20) | NOT NULL | "medium" | 危险等级 |
| location | VARCHAR(255) | NOT NULL | "" | 位置信息 |
| status | VARCHAR(20) | NOT NULL | "active" | 告警状态 |
| description | TEXT | NOT NULL | "" | 告警描述 |
| handled_by_id | BIGINT | FK → users.id, NULLABLE | NULL | 处理人 |
| processing_notes | TEXT | NOT NULL | "" | 处理记录 |
| created_at | DATETIME | NOT NULL | 自动生成 | 创建时间 |
| updated_at | DATETIME | NOT NULL | 自动更新 | 最后修改时间（auto_now，每次状态变更自动更新） |
| acknowledged_at | DATETIME | NULLABLE | NULL | 确认时间 |
| resolved_at | DATETIME | NULLABLE | NULL | 解决时间 |

**level 字段枚举值：**

| 值 | 含义 | 触发条件 |
|----|------|---------|
| low | 低危 | 匹配 1 个危机关键词 |
| medium | 中危 | 匹配 2 个危机关键词 / 默认值 |
| high | 高危 | 匹配 3+ 个危机关键词 |
| critical | 紧急 | 手动设定 |

**status 字段状态流转：**

```
active (待处理) ──→ acknowledged (已确认) ──→ resolved (已解决)
       │                                          ↑
       └──────────────────────────────────────────┘
                     (可直接解决)
```

| 值 | 含义 | 操作 |
|----|------|------|
| active | 待处理 | 新创建的告警默认状态 |
| acknowledged | 已确认 | 专业人员确认接收，需填写处理记录 |
| resolved | 已解决 | 标记完成处理，需填写处理记录 |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| crisis_alerts_user_id_215e1780 | user_id | 普通索引 |
| crisis_alerts_handled_by_id_2ea0ec80 | handled_by_id | 普通索引 |
| idx_crisis_status_created | (status, created_at) | 复合索引（优化按状态+时间查询告警） |

**排序规则：** `ORDER BY created_at DESC`

**外键约束：**
- user_id → users.id，级联删除（CASCADE）
- handled_by_id → users.id，置空（SET_NULL）

---

### 3.6 assessment_results（心理评估结果表）

存储标准化心理量表（PHQ-9 抑郁量表、GAD-7 焦虑量表）的评估结果，包含总分、逐题得分和严重度判定。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| user_id | BIGINT | FK → users.id, NOT NULL | — | 评估用户 |
| assessment_type | VARCHAR(10) | NOT NULL | — | 量表类型 |
| total_score | INTEGER | NOT NULL | — | 总分 |
| answers | TEXT (JSON) | NOT NULL | [] | 逐题得分数组 |
| severity | VARCHAR(30) | NOT NULL | "" | 严重度判定 |
| created_at | DATETIME | NOT NULL | 自动生成 | 评估时间 |

**assessment_type 枚举值：**

| 值 | 含义 | 题目数 | 分值范围 |
|----|------|--------|---------|
| phq9 | PHQ-9 抑郁量表 | 9 题 | 0-27 分 |
| gad7 | GAD-7 焦虑量表 | 7 题 | 0-21 分 |

**severity 严重度判定规则：**

PHQ-9：

| 总分 | severity 值 |
|------|------------|
| 0-4 | 正常 |
| 5-9 | 轻度 |
| 10-14 | 中度 |
| 15-19 | 中重度 |
| 20-27 | 重度 |

GAD-7：

| 总分 | severity 值 |
|------|------------|
| 0-4 | 正常 |
| 5-9 | 轻度 |
| 10-14 | 中度 |
| 15-21 | 重度 |

**answers 字段示例：**
```json
[2, 1, 3, 0, 1, 2, 1, 0, 1]  // PHQ-9 共9个值，每个值0-3
[1, 2, 0, 1, 1, 2, 0]        // GAD-7 共7个值，每个值0-3
```

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| assessment_results_user_id_2601163e | user_id | 普通索引 |
| idx_assess_user_type_created | (user, assessment_type, created_at) | 复合索引（优化按用户+量表类型查询评估历史） |

**排序规则：** `ORDER BY created_at DESC`

**外键约束：** user_id → users.id，级联删除（CASCADE）

---

### 3.7 patient_assignments（患者-专业人员分配表）

建立患者与专业人员的多对多分配关系，专业人员只能查看和管理被分配的患者数据。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| patient_id | BIGINT | FK → users.id, NOT NULL | — | 患者 |
| professional_id | BIGINT | FK → users.id, NOT NULL | — | 专业人员 |
| created_at | DATETIME | NOT NULL | 自动生成 | 分配时间 |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| uniq_patient_professional | (patient_id, professional_id) | UniqueConstraint 联合唯一 |
| patient_assignments_patient_id_6cfa4a64 | patient_id | 普通索引 |
| patient_assignments_professional_id_0999d881 | professional_id | 普通索引 |

**外键约束：**
- patient_id → users.id，级联删除（CASCADE）
- professional_id → users.id，级联删除（CASCADE）

**业务规则：** 仅 admin 角色可创建和删除分配关系。

---

### 3.8 supporter_links（守护者-患者关联表）

建立守护者（亲友）与患者的多对多关联关系，守护者可查看关联患者的脱敏状态摘要。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| supporter_id | BIGINT | FK → users.id, NOT NULL | — | 守护者 |
| patient_id | BIGINT | FK → users.id, NOT NULL | — | 患者 |
| created_at | DATETIME | NOT NULL | 自动生成 | 关联时间 |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| uniq_supporter_patient | (supporter_id, patient_id) | UniqueConstraint 联合唯一 |
| supporter_links_supporter_id_be1599bc | supporter_id | 普通索引 |
| supporter_links_patient_id_c1c90dde | patient_id | 普通索引 |

**外键约束：**
- supporter_id → users.id，级联删除（CASCADE）
- patient_id → users.id，级联删除（CASCADE）

**业务规则：** 守护者通过患者的用户名或手机号搜索并发起关联。

---

### 3.9 encouragement_messages（鼓励消息表）

守护者向关联患者发送的鼓励消息，支持未读计数和已读标记。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| sender_id | BIGINT | FK → users.id, NOT NULL | — | 发送者（守护者） |
| receiver_id | BIGINT | FK → users.id, NOT NULL | — | 接收者（患者） |
| content | TEXT | NOT NULL | — | 消息内容 |
| is_read | BOOLEAN | NOT NULL | False | 是否已读 |
| created_at | DATETIME | NOT NULL | 自动生成 | 发送时间 |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| encouragement_messages_sender_id_f23b75d6 | sender_id | 普通索引 |
| encouragement_messages_receiver_id_7b4864df | receiver_id | 普通索引 |
| idx_encourage_receiver_read | (receiver, is_read) | 复合索引（优化未读消息计数查询） |

**排序规则：** `ORDER BY created_at DESC`

**外键约束：**
- sender_id → users.id，级联删除（CASCADE）
- receiver_id → users.id，级联删除（CASCADE）

---

### 3.10 recovery_tasks（康复任务表）

每日自动生成的康复任务，从 6 种任务模板中随机选取 3 个，用户完成后打卡标记。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| user_id | BIGINT | FK → users.id, NOT NULL | — | 所属用户 |
| task_type | VARCHAR(20) | NOT NULL | — | 任务类型 |
| title | VARCHAR(100) | NOT NULL | — | 任务标题 |
| description | TEXT | NOT NULL | "" | 任务描述 |
| is_completed | BOOLEAN | NOT NULL | False | 是否已完成 |
| date | DATE | NOT NULL | — | 任务日期 |
| completed_at | DATETIME | NULLABLE | NULL | 完成时间 |
| created_at | DATETIME | NOT NULL | 自动生成 | 创建时间 |

**task_type 枚举值：**

| 值 | 含义 | 任务标题 | 任务描述 |
|----|------|---------|---------|
| mood_log | 情绪记录 | 记录今天的心情 | 花几分钟记录你此刻的情绪状态和想法 |
| mindfulness | 正念练习 | 5分钟正念呼吸 | 找一个安静的地方，专注于呼吸5分钟 |
| reading | 科普阅读 | 阅读一篇心理健康文章 | 在科普阅读中了解心理健康知识 |
| exercise | 运动锻炼 | 30分钟散步或运动 | 适度的身体运动有助于改善情绪 |
| social | 社交互动 | 和一个人聊天 | 与家人或朋友进行一次简短的交流 |
| gratitude | 感恩日记 | 写下3件感恩的事 | 记录今天让你感到感恩的三件小事 |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| recovery_tasks_user_id_9f5987cf | user_id | 普通索引 |
| idx_recovery_user_date | (user, date) | 复合索引（优化按用户+日期查询每日任务） |

**排序规则：** `ORDER BY date ASC, created_at ASC`

**外键约束：** user_id → users.id，级联删除（CASCADE）

**业务规则：**
- 每日首次访问时自动生成 3 个任务（从 6 种模板随机选取）
- 同一天不会重复生成（检查 date 字段），由 `uniq_user_date_tasktype` 数据库级唯一约束保护
- 完成任务时更新 is_completed=True 并记录 completed_at 时间戳

---

### 3.11 recovery_badges（成就徽章表）

成就系统徽章记录，通过 (user_id, badge_type) 联合唯一约束防止同一用户重复获得同一徽章。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| user_id | BIGINT | FK → users.id, NOT NULL | — | 获得用户 |
| badge_type | VARCHAR(30) | NOT NULL | — | 徽章类型 |
| earned_at | DATETIME | NOT NULL | 自动生成 | 获得时间 |

**badge_type 枚举值（共 9 种）：**

| 值 | 含义 | 获取条件 |
|----|------|---------|
| first_mood | 首次记录 | 首次创建情绪记录 |
| first_chat | 首次对话 | 首次完成 AI 对话 |
| first_assessment | 首次评估 | 首次完成心理评估 |
| tasks_10 | 完成10个任务 | 累计完成 10 个康复任务 |
| tasks_50 | 完成50个任务 | 累计完成 50 个康复任务 |
| streak_3 | 连续3天 | 连续 3 天完成任务打卡 |
| streak_7 | 连续7天 | 连续 7 天完成任务打卡 |
| streak_30 | 连续30天 | 连续 30 天完成任务打卡 |
| mood_improve | 情绪改善 | 近期平均情绪评分提升 |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| uniq_user_badge_type | (user_id, badge_type) | UniqueConstraint 联合唯一 |
| recovery_badges_user_id_2e9a1ce7 | user_id | 普通索引 |

**排序规则：** `ORDER BY earned_at DESC`

**外键约束：** user_id → users.id，级联删除（CASCADE）

---

### 3.12 articles（科普文章表）

心理健康科普文章，支持 7 种分类和外部链接。由管理员或专业人员创建和管理。

| 字段名 | 数据类型 | 约束 | 默认值 | 说明 |
|--------|---------|------|--------|------|
| id | INTEGER (BigAutoField) | PK, NOT NULL | 自增 | 主键 |
| title | VARCHAR(200) | NOT NULL | — | 文章标题 |
| summary | TEXT | NOT NULL | "" | 文章摘要 |
| content | TEXT | NOT NULL | — | 文章正文 |
| url | VARCHAR(500) | NOT NULL | "" | 外部链接（可选） |
| category | VARCHAR(30) | NOT NULL | "general" | 文章分类 |
| author_id | BIGINT | FK → users.id, NULLABLE | NULL | 作者 |
| is_published | BOOLEAN | NOT NULL | True | 是否发布 |
| created_at | DATETIME | NOT NULL | 自动生成 | 创建时间 |
| updated_at | DATETIME | NOT NULL | 自动更新 | 更新时间 |

**category 枚举值：**

| 值 | 含义 |
|----|------|
| depression | 抑郁症 |
| anxiety | 焦虑症 |
| sleep | 睡眠 |
| stress | 压力管理 |
| relationship | 人际关系 |
| self_care | 自我关怀 |
| general | 综合（默认） |

**索引：**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| articles_author_id_e11be98a | author_id | 普通索引 |
| idx_article_pub_category | (is_published, category) | 复合索引（优化按发布状态+分类筛选文章） |

**排序规则：** `ORDER BY created_at DESC`

**外键约束：** author_id → users.id，置空（SET_NULL）

**设计说明：**
- `url` 字段不为空时，患者端点击文章将直接跳转外部链接而非显示内容详情
- `author` 使用 SET_NULL 策略，作者账号删除后文章保留
- `updated_at` 使用 auto_now=True，每次保存自动更新

---

## 四、Django 框架表设计

### 4.1 auth_group（用户组表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| id | INTEGER | PK | 主键 |
| name | VARCHAR(150) | UNIQUE, NOT NULL | 组名 |

### 4.2 auth_permission（权限定义表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| id | INTEGER | PK | 主键 |
| content_type_id | INTEGER | FK → django_content_type.id, NOT NULL | 关联模型类型 |
| codename | VARCHAR(100) | NOT NULL | 权限代码 |
| name | VARCHAR(255) | NOT NULL | 权限名称 |

**唯一约束：** (content_type_id, codename)

### 4.3 auth_group_permissions（用户组-权限关联表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| id | INTEGER | PK | 主键 |
| group_id | INTEGER | FK → auth_group.id, NOT NULL | 用户组 |
| permission_id | INTEGER | FK → auth_permission.id, NOT NULL | 权限 |

**唯一约束：** (group_id, permission_id)

### 4.4 django_admin_log（管理后台操作日志表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| id | INTEGER | PK | 主键 |
| action_time | DATETIME | NOT NULL | 操作时间 |
| user_id | BIGINT | FK → users.id, NOT NULL | 操作用户 |
| content_type_id | INTEGER | FK → django_content_type.id, NULLABLE | 操作对象类型 |
| object_id | TEXT | NULLABLE | 操作对象 ID |
| object_repr | VARCHAR(200) | NOT NULL | 对象字符串表示 |
| action_flag | SMALLINT UNSIGNED | NOT NULL | 操作类型（1=新增, 2=修改, 3=删除） |
| change_message | TEXT | NOT NULL | 变更内容描述 |

### 4.5 django_content_type（模型内容类型注册表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| id | INTEGER | PK | 主键 |
| app_label | VARCHAR(100) | NOT NULL | 应用名称 |
| model | VARCHAR(100) | NOT NULL | 模型名称 |

**唯一约束：** (app_label, model)

### 4.6 django_migrations（数据库迁移记录表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| id | INTEGER | PK | 主键 |
| app | VARCHAR(255) | NOT NULL | 应用名称 |
| name | VARCHAR(255) | NOT NULL | 迁移文件名 |
| applied | DATETIME | NOT NULL | 执行时间 |

### 4.7 django_session（会话存储表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| session_key | VARCHAR(40) | PK | 会话密钥 |
| session_data | TEXT | NOT NULL | 加密的会话数据 |
| expire_date | DATETIME | NOT NULL, INDEX | 过期时间 |

---

## 五、多对多关联表设计

### 5.1 users_groups（用户-用户组关联表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| id | INTEGER | PK | 主键 |
| user_id | BIGINT | FK → users.id, NOT NULL | 用户 |
| group_id | INTEGER | FK → auth_group.id, NOT NULL | 用户组 |

**唯一约束：** (user_id, group_id)

### 5.2 users_user_permissions（用户-权限关联表）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|---------|------|------|
| id | INTEGER | PK | 主键 |
| user_id | BIGINT | FK → users.id, NOT NULL | 用户 |
| permission_id | INTEGER | FK → auth_permission.id, NOT NULL | 权限 |

**唯一约束：** (user_id, permission_id)

---

## 六、索引设计总结

系统共创建 **34 个索引**（含自动索引），按类型统计：

| 索引类型 | 数量 | 说明 |
|---------|------|------|
| 主键索引 | 21 | 每张表自动创建 |
| UNIQUE 唯一索引 | 9 | users(username, email, phone)、联合唯一约束等 |
| 普通索引（外键） | 18 | Django 自动为 ForeignKey 创建 |
| 业务索引 | 1 | chat_messages.session_id（显式 db_index=True） |
| 复合索引（优化） | 7 | 针对高频查询路径的多字段联合索引 |

**7 个复合索引详细说明：**

| 索引名 | 表 | 字段 | 优化的查询场景 |
|--------|---|------|--------------|
| idx_chat_user_session | chat_messages | (user, session_id, created_at) | 按用户+会话加载对话历史，避免全表扫描 |
| idx_crisis_status_created | crisis_alerts | (status, created_at) | 活跃告警实时查询（紧急响应系统每5秒轮询） |
| idx_encourage_receiver_read | encouragement_messages | (receiver, is_read) | 患者端未读鼓励消息计数 |
| idx_recovery_user_date | recovery_tasks | (user, date) | 每日康复任务查询和连续打卡检测 |
| idx_article_pub_category | articles | (is_published, category) | 患者端按分类浏览已发布文章 |
| idx_mood_user_created | mood_entries | (user, created_at) | 情绪趋势图查询、按时间排序取最近记录 |
| idx_assess_user_type_created | assessment_results | (user, assessment_type, created_at) | 按用户+量表类型查询评估历史 |

**关键查询路径的索引覆盖：**

| 查询场景 | 使用索引 |
|---------|---------|
| 按用户查询情绪记录 | idx_mood_user_created（复合索引） |
| 情绪趋势图（按时间聚合） | idx_mood_user_created（复合索引） |
| 按会话加载对话消息 | idx_chat_user_session（复合索引） |
| 查询活跃/已确认告警 | idx_crisis_status_created（复合索引） |
| 查询用户的活跃告警 | crisis_alerts_user_id + status 过滤 |
| 按量表类型查评估记录 | idx_assess_user_type_created（复合索引） |
| 查询守护者关联的患者 | supporter_links_supporter_id |
| 未读鼓励消息计数 | idx_encourage_receiver_read（复合索引） |
| 按日期查询康复任务 | idx_recovery_user_date（复合索引） |
| 按分类浏览文章 | idx_article_pub_category（复合索引） |
| 检查徽章是否已颁发 | uniq_user_badge_type（联合唯一约束） |

---

## 七、数据完整性与安全设计

### 7.1 外键删除策略

| 策略 | 应用场景 | 说明 |
|------|---------|------|
| CASCADE（级联删除） | 用户相关的所有业务数据 | 用户注销时自动删除其全部情绪记录、对话、告警、评估、任务、徽章、关联关系 |
| SET_NULL（置空） | articles.author_id、crisis_alerts.handled_by_id | 作者/处理人删除后，文章和告警记录保留，外键置 NULL |

### 7.2 唯一性约束

| 约束 | 表 | 字段 | 目的 |
|------|---|------|------|
| 单字段唯一 | users | username | 用户名不可重复 |
| 单字段唯一 | users | email | 邮箱不可重复 |
| 单字段唯一 | users | phone | 手机号不可重复 |
| 单字段唯一 | chat_feedbacks | message_id | 每条消息最多一个反馈 |
| UniqueConstraint | patient_assignments | (patient_id, professional_id) | 防止重复分配 |
| UniqueConstraint | supporter_links | (supporter_id, patient_id) | 防止重复关联 |
| UniqueConstraint | recovery_badges | (user_id, badge_type) | 同一徽章不可重复颁发 |
| UniqueConstraint | recovery_tasks | (user_id, date, task_type) | 防止同一天同类型任务重复 |

### 7.3 数据隐私保护

| 保护措施 | 实现方式 |
|---------|---------|
| 密码安全 | Django PBKDF2_SHA256 哈希存储，不可逆 |
| 数据隔离 | 基于 role 的 API 权限控制，患者只能访问自己的数据 |
| 脱敏访问 | 守护者仅可查看患者状态摘要（最近情绪、活跃告警数），不可查看对话内容 |
| 账号注销 | CASCADE 级联删除，永久清除用户所有关联数据 |
| 数据导出 | 仅 admin 角色可导出 CSV 数据 |

---

## 八、迁移记录

| 迁移文件 | 操作说明 |
|----------|---------|
| 0001_initial | 创建 User、MoodEntry、ChatMessage、CrisisAlert 四张核心表 |
| 0002_alter_user_phone | phone 字段添加 UNIQUE 约束 |
| 0003_alter_user_role | role 字段新增 supporter 选项 |
| 0004_crisis_alert_workflow | CrisisAlert 新增 handled_by、acknowledged_at、resolved_at 字段 |
| 0005_add_processing_notes | CrisisAlert 新增 processing_notes 处理记录字段 |
| 0006_add_chat_feedback | 新建 ChatFeedback 表 |
| 0007_add_assessment_result | 新建 AssessmentResult 表 |
| 0008_add_supporter_and_encouragement | 新建 SupporterLink、EncouragementMessage 两张表 |
| 0009_add_recovery_plan | 新建 RecoveryTask、RecoveryBadge 两张表 |
| 0010_add_article | 新建 Article 表 |
| 0011_add_article_url | Article 新增 url 字段 |
| 0012_add_composite_indexes_and_validators | 新增 5 个复合索引（idx_chat_user_session、idx_crisis_status_created、idx_encourage_receiver_read、idx_recovery_user_date、idx_article_pub_category），MoodEntry.mood_score 新增数据库级验证器 |
| 0013_optimize_constraints_indexes_and_fields | 架构评审优化：email 字段改为 NULLABLE 防空字符串唯一冲突；CrisisAlert 新增 updated_at 审计字段；新增 2 个复合索引（idx_mood_user_created、idx_assess_user_type_created）；3 处 unique_together 升级为 UniqueConstraint；RecoveryTask 新增 (user, date, task_type) 唯一约束 |

---

## 九、数据库设计评审与优化记录

> 本节记录对数据库设计的两轮系统性评审与优化，包括已识别的问题、已实施的优化措施，以及面向未来演进的架构建议。

### 9.1 设计评审概述

评审维度覆盖 **数据安全、约束完整性、索引策略、查询效率、API 规范、架构扩展性** 六个层面，从 12 张业务表、1355 行视图代码、218 行序列化代码中识别出 10 类问题，分两轮迭代修复。

### 9.2 第一轮优化（迁移 0012）— 索引与查询优化

**问题诊断：** 通过逐行审计 views.py 中的所有 ORM 查询，发现以下性能瓶颈：

| 问题编号 | 问题类型 | 位置 | 影响 |
|---------|---------|------|------|
| OPT-01 | 缺少复合索引 | chat_messages 表 | 按用户+会话+时间的三字段查询无法利用索引，全表扫描 |
| OPT-02 | 缺少复合索引 | crisis_alerts 表 | 紧急响应系统每 5 秒轮询活跃告警，仅按 status 过滤但无复合索引 |
| OPT-03 | 缺少复合索引 | encouragement_messages 表 | 未读消息计数查询需扫描全表 |
| OPT-04 | 缺少复合索引 | recovery_tasks 表 | 每日任务查询和连续打卡检测效率低 |
| OPT-05 | 缺少复合索引 | articles 表 | 按发布状态+分类筛选无索引 |
| OPT-06 | 缺少字段验证 | mood_entries.mood_score | 仅有序列化器层验证，无数据库级约束 |
| OPT-07 | N+1 查询 | chat_sessions 视图 | 每个会话单独查首条消息，N 个会话 = N+1 次查询 |
| OPT-08 | N+1 查询 | patient_detail 视图 | 同上模式，加上 crisis_alerts 缺少 select_related |
| OPT-09 | 查询爆炸 | admin_dashboard_stats | 13 次独立 COUNT 查询聚合统计数据 |
| OPT-10 | 算法低效 | _calc_streak（连续打卡） | 逐天查询数据库判断是否完成，最多 365 次查询 |
| OPT-11 | 冗余删除 | delete_my_account | 手动删除 9 张关联表后再删用户，CASCADE 已自动处理 |

**已实施的优化措施：**

| 编号 | 优化措施 | 效果 |
|------|---------|------|
| OPT-01~05 | 新增 5 个复合索引 | 高频查询路径全部覆盖索引 |
| OPT-06 | MoodEntry.mood_score 添加 MinValueValidator/MaxValueValidator | 数据库级约束防止无效分值 |
| OPT-07~08 | 批量查询替代 N+1 | 1 次查询获取所有会话首条消息，用 Python dict 去重 |
| OPT-09 | 条件聚合重写 | 13 次查询 → 4 次条件聚合（Count + filter=Q()） |
| OPT-10 | 单次查询重写 | 一次查询最近 30 天已完成日期集合，内存中计算连续天数 |
| OPT-11 | 依赖 CASCADE | 删除 9 行冗余手动删除代码，仅调用 user.delete() |

### 9.3 第二轮优化（迁移 0013）— 架构级评审与修复

**P0 — 数据安全隐患**

| 问题编号 | 问题 | 严重性 | 修复状态 |
|---------|------|--------|---------|
| SEC-01 | `users.email` 使用 `unique=True, default=""` — 两个不填邮箱的用户同时 `email=""` 将触发 UNIQUE 约束冲突，导致注册崩溃。`phone` 字段正确使用了 `null=True, default=None` 规避此问题，但 `email` 未做同样处理 | 严重 | **已修复** — 改为 `null=True, default=None`，序列化器层将空邮箱规范化为 `None` |
| SEC-02 | `CrisisAlert` 缺少通用 `updated_at` 字段 — 危机告警是系统最关键的实体，状态流转仅记录特定节点时间戳（acknowledged_at、resolved_at），缺少通用审计时间戳追踪任意修改 | 重要 | **已修复** — 新增 `updated_at = DateTimeField(auto_now=True)` |

**P1 — 设计规范问题**

| 问题编号 | 问题 | 修复状态 |
|---------|------|---------|
| STD-01 | 3 个模型使用已废弃的 `unique_together` — Django 4.2+ 官方推荐 `constraints = [UniqueConstraint(...)]`，`unique_together` 已标记 deprecated | **已修复** — PatientAssignment、SupporterLink、RecoveryBadge 全部升级为 UniqueConstraint |
| STD-02 | `AssessmentResult` 缺少 `(user, assessment_type)` 复合索引 — views.py:190-195 按用户+量表类型查询是高频操作，仅有单字段 user_id 索引 | **已修复** — 新增 idx_assess_user_type_created 三字段复合索引 |
| STD-03 | `MoodEntry` 缺少 `(user, created_at)` 复合索引 — 情绪趋势图是核心功能，此查询组合在 views.py 中出现 5+ 次 | **已修复** — 新增 idx_mood_user_created 复合索引 |

**P2 — 数据完整性缺口**

| 问题编号 | 问题 | 修复状态 |
|---------|------|---------|
| INT-01 | `RecoveryTask` 缺少 `(user, date, task_type)` 唯一约束 — `_generate_daily_tasks` 仅靠应用层 `count > 0` 防重复，并发请求可穿透此检查 | **已修复** — 新增 uniq_user_date_tasktype 数据库级唯一约束 |
| INT-02 | `AssessmentResult.answers` 的 JSONField 无 schema 验证 — PHQ-9 应严格为 9 元素数组，GAD-7 应为 7 元素数组，但数据库层无校验 | 未修复（需应用层验证，非数据库层能解决） |
| INT-03 | 状态机仅在视图层实现 — CrisisAlert 的 active→acknowledged→resolved 流转无数据库级约束，直接 SQL 可绕过 | 未修复（SQLite 不支持 CHECK 约束引用其他行） |

### 9.4 架构级改进建议（未来演进方向）

以下为当前架构的已知局限和推荐的演进路线，标记为「建议」不作为当前版本缺陷：

| 建议编号 | 建议内容 | 优先级 | 说明 |
|---------|---------|--------|------|
| ARC-01 | **SQLite → PostgreSQL 迁移** | 高 | SQLite 单写者锁在并发场景（AI 对话写入 + 危机告警轮询 + 任务生成）形成瓶颈。PostgreSQL 支持行级锁、MVCC、CHECK 约束，可解决 INT-03 等问题 |
| ARC-02 | **引入软删除机制** | 中 | 心理健康数据具有医疗属性，当前 CASCADE 全量硬删除不符合医疗数据保留惯例。建议对 CrisisAlert、AssessmentResult 等关键表增加 `is_deleted` + `deleted_at` 字段 |
| ARC-03 | **ChatMessage 表分区/归档** | 低 | AI 对话消息增长最快（每次对话产生 2 条记录），长期运行后该表将成为最大的存储和查询压力源。建议按月份分区或定期归档历史数据 |
| ARC-04 | **Session 模型独立化** | 低 | 当前 chat_messages 通过 session_id UUID 隐式分组，无独立的 Session 实体。建议抽取 ChatSession 模型存储会话元数据（标题、创建时间、消息数），减少每次列表查询的聚合开销 |
| ARC-05 | **User 模型角色验证** | 低 | 当前四种角色共享同一张表，数据库层无法阻止「supporter 角色拥有 mood_entries」等逻辑矛盾。建议在关键外键处增加应用层 CHECK 或在 save() 中验证 |

### 9.5 优化效果总结

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 复合索引数 | 0 | 7 |
| UniqueConstraint 数 | 0（全部使用 deprecated unique_together） | 4 |
| admin_dashboard_stats 查询数 | 13 | 4 |
| 连续打卡检测查询数 | 最多 365 | 1 |
| chat_sessions N+1 查询 | N+1 | 2（固定） |
| 数据安全隐患 | email 空字符串唯一冲突、CrisisAlert 无审计时间戳 | 已修复 |
| 数据完整性缺口 | RecoveryTask 可并发重复生成 | 数据库级唯一约束保护 |
