# 数据库设计

## 1. 概述

后端使用 Django ORM 管理数据模型。默认本地数据库为 SQLite；当配置 `DB_ENGINE` 或 `DB_NAME` 等环境变量时，可切换到 PostgreSQL。当前核心业务模型定义在 `backend/api/models.py`。

## 2. 当前业务表

| 模型 | 表名 | 说明 |
| --- | --- | --- |
| `User` | `users` | 自定义用户表，继承 `AbstractUser`，扩展手机号、角色和创建时间 |
| `MoodEntry` | `mood_entries` | 患者情绪记录，支持软删除 |
| `ChatMessage` | `chat_messages` | AI 对话消息，按 `session_id` 管理会话，支持软删除 |
| `ChatFeedback` | `chat_feedbacks` | 用户对 AI 回复的点赞/点踩反馈 |
| `CrisisAlert` | `crisis_alerts` | 危机告警，支持处理状态和软删除 |
| `AssessmentResult` | `assessment_results` | PHQ-9 / GAD-7 评估结果 |
| `PatientAssignment` | `patient_assignments` | 患者与专业人员分配关系 |
| `SupporterLink` | `supporter_links` | 守护者与患者绑定关系 |
| `EncouragementMessage` | `encouragement_messages` | 守护者鼓励消息，也复用于患者-守护者私聊 |
| `RecoveryTask` | `recovery_tasks` | 系统自动生成的康复任务 |
| `RecoveryBadge` | `recovery_badges` | 患者获得的康复徽章 |
| `Article` | `articles` | 科普文章 |

> 如果统计 Django 内置认证表、迁移表、会话表等，数据库总表数会多于业务模型数。本文件只描述当前代码中的核心业务表。

## 3. 表结构说明

### 3.1 users

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `username` | varchar | 登录账号，唯一 |
| `password` | varchar | Django 哈希密码 |
| `email` | EmailField | 邮箱，可为空，唯一 |
| `phone` | varchar(20) | 手机号，可为空，唯一 |
| `role` | varchar(20) | `patient`、`professional`、`supporter`、`admin` |
| `created_at` | datetime | 创建时间 |

角色说明：

| role | 说明 |
| --- | --- |
| `patient` | 患者 |
| `professional` | 专业人员 |
| `supporter` | 守护者/亲友 |
| `admin` | 系统管理员 |

### 3.2 mood_entries

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `user_id` | FK -> users | 患者 |
| `mood_score` | integer | 1-10 分，序列化器和模型校验 |
| `content` | text | 情绪记录内容，可为空 |
| `created_at` | datetime | 创建时间 |
| `is_deleted` | boolean | 软删除标记 |
| `deleted_at` | datetime | 删除时间 |

索引：

- `idx_mood_user_created(user, created_at)`：优化患者历史情绪记录和趋势展示查询。
- `is_deleted` 索引：软删除过滤。

### 3.3 chat_messages

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `user_id` | FK -> users | 消息所属用户 |
| `content` | text | 消息正文 |
| `is_ai_response` | boolean | 是否为 AI 回复 |
| `session_id` | UUID | 会话 ID |
| `created_at` | datetime | 创建时间 |
| `is_deleted` | boolean | 软删除标记 |
| `deleted_at` | datetime | 删除时间 |

索引：

- `idx_chat_user_session(user, session_id, created_at)`：优化会话消息查询。

### 3.4 chat_feedbacks

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `message_id` | OneToOne -> chat_messages | 被反馈的 AI 消息 |
| `user_id` | FK -> users | 反馈用户 |
| `is_positive` | boolean | 点赞或点踩 |
| `created_at` | datetime | 创建时间 |

### 3.5 crisis_alerts

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `user_id` | FK -> users | 触发告警的患者 |
| `level` | varchar(20) | `low`、`medium`、`high`、`critical` |
| `location` | varchar(255) | 位置信息，可为空 |
| `status` | varchar(20) | `active`、`acknowledged`、`resolved` |
| `description` | text | 告警描述，通常保存触发文本或 SOS 描述 |
| `handled_by_id` | FK -> users | 处理人，可为空 |
| `processing_notes` | text | 处理备注 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |
| `acknowledged_at` | datetime | 确认时间 |
| `resolved_at` | datetime | 解决时间 |
| `is_deleted` | boolean | 软删除标记 |
| `deleted_at` | datetime | 删除时间 |

索引：

- `idx_crisis_status_created(status, created_at)`：优化活跃告警查询。
- `idx_crisis_user_status(user, status)`：优化患者告警状态查询。

当前 AI 危机检测会根据关键词命中数量返回 `low`、`medium`、`high`，模型保留 `critical` 以供人工或后续规则扩展。

### 3.6 assessment_results

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `user_id` | FK -> users | 患者 |
| `assessment_type` | varchar(10) | `phq9` 或 `gad7` |
| `total_score` | integer | 总分 |
| `answers` | JSONField | 各题得分数组 |
| `severity` | varchar(30) | 前端计算并提交的严重度文本 |
| `created_at` | datetime | 创建时间 |

索引：

- `idx_assess_user_type_created(user, assessment_type, created_at)`：优化评估历史查询。

当前代码没有数据库级 JSON schema 校验，PHQ-9 题数和 GAD-7 题数由前端流程保证。

### 3.7 patient_assignments

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `patient_id` | FK -> users | 患者 |
| `professional_id` | FK -> users | 专业人员 |
| `created_at` | datetime | 创建时间 |

约束：

- `uniq_patient_professional(patient, professional)`：防止重复分配。

### 3.8 supporter_links

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `supporter_id` | FK -> users | 守护者 |
| `patient_id` | FK -> users | 患者 |
| `created_at` | datetime | 创建时间 |

约束：

- `uniq_supporter_patient(supporter, patient)`：防止重复绑定。

### 3.9 encouragement_messages

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `sender_id` | FK -> users | 发送者 |
| `receiver_id` | FK -> users | 接收者 |
| `content` | text | 消息内容 |
| `is_read` | boolean | 是否已读 |
| `created_at` | datetime | 创建时间 |

索引：

- `idx_encourage_receiver_read(receiver, is_read)`：未读消息查询。
- `idx_encourage_sender_created(sender, created_at)`：发送历史查询。

### 3.10 recovery_tasks

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `user_id` | FK -> users | 患者 |
| `task_type` | varchar(20) | 任务类型 |
| `title` | varchar(100) | 标题 |
| `description` | text | 描述 |
| `is_completed` | boolean | 是否完成 |
| `date` | date | 任务日期 |
| `completed_at` | datetime | 完成时间 |
| `created_at` | datetime | 创建时间 |

任务类型包括：`mood_log`、`mindfulness`、`reading`、`exercise`、`social`、`gratitude`。

约束和索引：

- `uniq_user_date_tasktype(user, date, task_type)`：防止同日同类型重复任务。
- `idx_recovery_user_date(user, date)`：按日期查询任务。
- `idx_recovery_user_comp_date(user, is_completed, date)`：统计完成情况。

### 3.11 recovery_badges

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `user_id` | FK -> users | 患者 |
| `badge_type` | varchar(30) | 徽章类型 |
| `earned_at` | datetime | 获得时间 |

徽章类型包括：`streak_3`、`streak_7`、`streak_30`、`first_mood`、`first_chat`、`first_assessment`、`tasks_10`、`tasks_50`、`mood_improve`。

约束：

- `uniq_user_badge_type(user, badge_type)`：同一徽章不重复发放。

### 3.12 articles

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BigAutoField | 主键 |
| `title` | varchar(200) | 标题 |
| `summary` | text | 摘要，最长 1000 字符 |
| `content` | text | 正文 |
| `url` | URLField | 外部链接，可为空 |
| `category` | varchar(30) | 分类 |
| `author_id` | FK -> users | 作者，可为空 |
| `is_published` | boolean | 是否发布 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

分类包括：`depression`、`anxiety`、`sleep`、`stress`、`relationship`、`self_care`、`general`。

索引：

- `idx_article_pub_category(is_published, category)`：优化患者端分类浏览。


## 4. 权限和数据访问约束

- 患者只能创建和查看自己的情绪、评估、对话、康复数据。
- 专业人员只能查看分配给自己的患者数据。
- 管理员可以查看和管理平台数据。
- 守护者只能查看已绑定患者的脱敏状态摘要，并与已绑定患者通信。
- 告警处置接口仅限管理员和专业人员。
- 用户删除账号采用匿名化处理，而不是直接删除所有历史业务数据。


