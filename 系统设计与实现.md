# 心理守护 — AI 抑郁症干预与危机管理平台

# 系统设计与实现

> 最后更新：2026-03-16

---

## 一、引言

抑郁症是全球范围内最常见的精神障碍之一。据世界卫生组织（WHO）2023 年数据，全球约有 3.8% 的人口受到抑郁症影响，中国抑郁症患者约 9500 万人，但就诊率不足 10%。传统心理健康服务面临专业人员短缺、就诊门槛高、患者主动求助意愿低等困境。

近年来，大语言模型（LLM）技术的突破性进展为心理健康领域提供了新的可能。基于 AI 的对话干预系统能够提供 24 小时不间断的情感支持，降低患者的求助心理门槛，同时结合标准化心理评估量表和危机预警机制，可以构建一个完整的抑郁症数字化干预与管理平台。

本项目「心理守护」旨在设计并实现一个集 **AI 心理对话、情绪追踪、标准化心理评估、危机预警与响应、康复计划管理、亲友支持网络** 于一体的综合性平台，覆盖抑郁症患者、心理健康专业人员、系统管理员、患者亲友四类用户角色，形成完整的干预与管理闭环。

---

## 二、相关理论与技术基础

### 2.1 核心理论基础

#### 2.1.1 认知行为疗法（CBT）

认知行为疗法（Cognitive Behavioral Therapy）是目前循证研究最为充分的抑郁症心理治疗方法之一。CBT 的核心理论认为，个体的情绪和行为受到其认知方式的影响——消极的自动思维（Automatic Thoughts）和认知歪曲（Cognitive Distortions）是导致和维持抑郁症状的关键因素。

本系统在以下模块中融入了 CBT 理论：
- **AI 对话模块**：AI 助手采用共情式对话策略，引导用户觉察和表达情绪，帮助用户识别消极思维模式，这是 CBT 的核心技术之一；
- **情绪记录模块**：鼓励用户每日记录情绪状态和伴随想法，对应 CBT 中的"情绪日记"（Mood Diary）技术；
- **康复计划模块**：通过每日任务（正念练习、感恩日记、社交互动等）引导行为激活（Behavioral Activation），这是 CBT 治疗抑郁症的关键组成部分。

#### 2.1.2 PHQ-9 与 GAD-7 标准化评估量表

**PHQ-9（Patient Health Questionnaire-9）** 是由 Kroenke 等人于 2001 年开发的 9 项自评量表，用于筛查和评估抑郁症严重程度。该量表基于 DSM-IV 抑郁症诊断标准，每个条目评分 0-3 分（0=完全没有，1=有几天，2=超过一半天数，3=几乎每天），总分范围 0-27 分。严重度分级如下：

| 分数范围 | 严重程度 | 临床建议 |
|---------|---------|---------|
| 0-4 | 正常 | 无需干预 |
| 5-9 | 轻度抑郁 | 关注观察，生活方式调整 |
| 10-14 | 中度抑郁 | 建议心理咨询 |
| 15-19 | 中重度抑郁 | 建议专业治疗 |
| 20-27 | 重度抑郁 | 需紧急专业干预 |

**GAD-7（Generalized Anxiety Disorder-7）** 是由 Spitzer 等人于 2006 年开发的 7 项焦虑筛查量表。由于抑郁症与焦虑症高度共病（约 60% 的抑郁症患者同时存在焦虑症状），系统同时集成 GAD-7 量表以全面评估患者状态。评分规则与 PHQ-9 一致，总分范围 0-21 分，5/10/15 分为分级切点。

#### 2.1.3 危机干预理论

危机干预（Crisis Intervention）理论认为，心理危机是一种时间有限的状态，及时的干预可以有效防止自杀等极端行为。本系统采用的三级危机响应模型参考了 Roberts 的七阶段危机干预模型：

1. **自动检测层**：基于关键词匹配算法实时分析用户对话内容，识别自杀/自伤相关表述；
2. **分级告警层**：根据关键词匹配数量进行风险等级分级（低/中/高），生成对应级别的危机告警；
3. **人工响应层**：专业人员通过紧急响应系统实时接收告警，执行确认-处理-解决的标准化响应流程。

#### 2.1.4 社会支持理论

社会支持理论（Social Support Theory）指出，来自家庭和朋友的情感支持是抑郁症康复的重要保护因素。系统设计了亲友支持模块（守护者模式），允许患者的亲友关联患者、查看脱敏的状态摘要、发送鼓励消息，在保护患者隐私的前提下构建社会支持网络。

### 2.2 关键技术概述

#### 2.2.1 开发与研究工具

**后端技术栈**

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.9+ | 后端开发语言 |
| Django | 4.2 | Web 框架，ORM，数据持久化 |
| Django REST Framework | 3.14+ | RESTful API 构建 |
| Simple JWT | 5.3+ | JWT 认证与令牌管理 |
| Flask | 3.0+ | AI 微服务框架 |
| Gunicorn | — | WSGI 生产服务器 |
| SQLite | 3 | 关系型数据库 |

**前端技术栈**

| 技术 | 版本 | 用途 |
|------|------|------|
| React Native (Expo) | SDK 52 | 患者端跨平台移动应用（部署为 Web） |
| React | 19.0 | 管理后台 SPA 框架 |
| Vue 3 | 3.4 | 紧急响应系统 SPA 框架 |
| TypeScript | 5.6 | 前端类型安全开发语言 |
| Vite | 6.0 | 前端构建工具 |
| Axios | 1.7+ | HTTP 客户端 |
| React Navigation | 7.0 | 移动端导航管理 |

**AI 与语音技术**

| 技术 | 说明 |
|------|------|
| Claude Haiku 4.5 | Anthropic 大语言模型，用于心理对话 |
| Web Speech API | 浏览器原生语音识别（STT），零成本 |
| edge-tts | 7.0+ | Microsoft Edge 免费 TTS 服务，中文女声（zh-CN-XiaoxiaoNeural） |

**部署与运维**

| 技术 | 说明 |
|------|------|
| Nginx | 反向代理与静态文件服务 |
| systemd | 进程管理（Django、Flask 服务守护） |
| 阿里云 ECS | 云服务器（Ubuntu 22.04，2 vCPU，1.6GB RAM） |

#### 2.2.2 相关技术原理

**（1）大语言模型（LLM）与心理对话**

本系统采用 Anthropic Claude Haiku 4.5 作为 AI 对话引擎。Claude 模型基于 Constitutional AI（CAI）训练方法，在安全性和有益性方面经过专门优化，适合心理健康等敏感场景。

系统通过精心设计的 System Prompt 约束 AI 行为：
- 以温暖、共情、非评判的态度倾听用户
- 引导用户探索情绪，而非直接给出建议
- 明确声明不替代专业心理治疗或药物处方
- 在识别到高风险信号时推荐危机热线（400-161-9995）
- 使用中文回复，控制回复长度为 2-4 句，保持朋友式的亲切语气

对话采用多轮上下文机制，每次请求携带最近 20 条历史消息，使 AI 能够理解对话脉络。

**（2）Server-Sent Events（SSE）流式传输**

传统 HTTP 请求-响应模式下，AI 生成的长文本需要等待完整生成后才能返回，导致用户等待时间过长。系统采用 SSE 技术实现流式传输：

```
客户端发送消息 → Django 后端转发至 Flask AI 服务
    → Flask 调用 Claude API (stream=True)
    → 逐 token 生成回复
    → SSE 格式推送: data: {"delta": "你"}
    → SSE 格式推送: data: {"delta": "好"}
    → ...
    → Django StreamingHttpResponse 透传至客户端
    → 前端实时拼接显示（打字机效果）
```

SSE 基于 HTTP 长连接，相较 WebSocket 更轻量，且天然支持 HTTP 代理穿透，适合单向数据流场景。

**（3）JWT（JSON Web Token）认证机制**

系统采用 JWT 实现无状态认证，避免服务端维护 Session 的开销：

- **Access Token**：有效期 7 天，携带用户 ID 和角色信息，每次 API 请求通过 `Authorization: Bearer <token>` 头部传递；
- **Refresh Token**：有效期 30 天，用于在 Access Token 过期后换取新令牌，采用 Token 轮换策略（Rotate Refresh Tokens），每次刷新生成新的 Refresh Token，已使用的旧 Token 失效，提升安全性；
- **角色权限控制**：Token Payload 中包含用户角色（patient/professional/supporter/admin），后端 API 基于角色实施细粒度访问控制。

**（4）危机关键词检测算法**

系统采用基于关键词匹配的危机检测方法。该方法虽然简单，但具有零延迟、零误报可解释性强的优点，适合作为第一道安全屏障：

```
输入: 用户消息文本
关键词库: [想死, 自杀, 不想活, 结束生命, 跳楼, 割腕, 去死,
          活不下去, 了结, 轻生, 死了算了, 没有意义, 不想活了,
          服药自杀, 上吊, 跳河, 跳桥, 死掉, 寻死]  (共19个)

算法:
  matched = [kw for kw in 关键词库 if kw in 用户消息]
  if len(matched) >= 3: risk_level = "high"
  elif len(matched) == 2: risk_level = "medium"
  elif len(matched) == 1: risk_level = "low"
  else: risk_level = "none"
```

检测到危机信号后，系统自动创建 `CrisisAlert` 记录并触发实时告警推送。

**（5）Web Speech API 语音识别**

Web Speech API 是 W3C 规范定义的浏览器原生语音识别接口，Chrome 浏览器已提供完整实现。系统通过该 API 实现零成本的语音输入：

```typescript
const recognition = new webkitSpeechRecognition();
recognition.lang = 'zh-CN';        // 中文识别
recognition.continuous = true;       // 持续识别模式
recognition.interimResults = true;   // 返回中间结果（实时显示）
```

识别过程中同时返回中间结果（interimResults）和最终结果（finalResults），前端将两者拼接后实时更新输入框内容，实现"边说边显示"的用户体验。

**（6）edge-tts 文字转语音**

edge-tts 是一个开源 Python 库，调用 Microsoft Edge 浏览器内置的免费 TTS 接口。相较商业 TTS 服务，其优势为：零成本、无需 API Key、中文语音质量高（采用 zh-CN-XiaoxiaoNeural 神经网络语音）、服务器内存占用极小（<50MB）。

系统通过 Flask 服务封装 TTS 端点，前端调用后获取 MP3 音频流，使用 HTML5 Audio API 播放。

---

## 三、系统/方案设计

### 3.1 需求分析

#### 3.1.1 功能需求

基于抑郁症数字化干预的实际需求，系统面向四类用户角色，需实现以下核心功能：

**患者端功能需求（天使端）**

| 编号 | 功能模块 | 需求描述 |
|------|---------|---------|
| F-P01 | 情绪记录 | 用户可通过表情选择器（五档）和精确分数（1-10分）记录每日情绪状态，附加文字描述想法和感受 |
| F-P02 | 情绪趋势 | 以柱状图可视化展示近期情绪变化趋势，支持按今日/本月/本年/全部切换时间范围 |
| F-P03 | AI 对话 | 与 AI 心理助手进行文字对话，支持流式回复（打字机效果）、语音输入、AI 回复语音播放 |
| F-P04 | 对话历史 | 管理多个对话会话，可新建对话或加载历史对话记录 |
| F-P05 | 心理评估 | 完成 PHQ-9 抑郁量表和 GAD-7 焦虑量表，自动评分与严重度判定，查看历史评估记录 |
| F-P06 | 今日计划 | 每日自动生成 3 个康复任务（情绪记录/正念呼吸/阅读/运动/社交/感恩日记），完成任务打卡 |
| F-P07 | 成就系统 | 连续打卡奖励、里程碑徽章（共 9 种），激励用户持续参与康复过程 |
| F-P08 | 科普阅读 | 浏览心理健康文章，支持 7 种分类筛选，支持外部链接跳转 |
| F-P09 | 紧急求助 | 一键 SOS 紧急求助，内置危机热线和自定义紧急联系人，支持通讯录导入 |
| F-P10 | 隐私管理 | 查看个人信息、隐私设置、注销账号（永久删除所有数据） |

**守护者端功能需求（亲友端）**

| 编号 | 功能模块 | 需求描述 |
|------|---------|---------|
| F-S01 | 关联患者 | 通过用户名或手机号关联天使（患者），建立守护关系 |
| F-S02 | 状态查看 | 查看关联天使的脱敏状态摘要（最近情绪、对话数、活跃告警） |
| F-S03 | 发送鼓励 | 向关联天使发送预设或自定义鼓励消息 |

**管理后台功能需求**

| 编号 | 功能模块 | 需求描述 |
|------|---------|---------|
| F-A01 | 患者管理 | 查看患者列表（搜索/分页/CSV导出），点击查看患者详情全景 |
| F-A02 | 患者详情 | 展示患者完整信息：情绪趋势（支持时间范围切换）、对话摘要、告警历史、评估记录 |
| F-A03 | 危机监控 | 实时轮询活跃告警，统计栏可点击筛选类型，超时告警高亮，确认接收/标记已解决 |
| F-A04 | 告警历史 | 全部告警记录，支持级别/状态/日期筛选 |
| F-A05 | 数据概览 | 用户/情绪/对话/告警统计仪表盘，CSV 数据导出 |
| F-A06 | 干预建议 | 12 个按风险等级分类的专业干预方案库 |
| F-A07 | 内容管理 | 文章 CRUD，支持外部链接，分类筛选和搜索 |
| F-A08 | 用户管理 | 所有用户列表，角色筛选，切换活跃状态，修改角色（admin 专属） |
| F-A09 | 分配管理 | 创建/删除患者-专业人员分配关系（admin 专属） |

**紧急响应系统功能需求**

| 编号 | 功能模块 | 需求描述 |
|------|---------|---------|
| F-E01 | 实时监控 | 每 5 秒轮询活跃告警，无需登录 |
| F-E02 | 告警展示 | 按危险等级颜色编码，NEW 徽章标识新告警，超时告警闪烁高亮 |
| F-E03 | 响应操作 | 确认接收、标记已解决，需填写处理记录 |

#### 3.1.2 非功能需求

| 类别 | 需求描述 |
|------|---------|
| **性能** | AI 对话首字响应时间 <3 秒；危机告警创建到前端展示延迟 <10 秒（5 秒轮询周期）；页面首屏加载时间 <3 秒 |
| **安全性** | JWT 认证 + Token 轮换防止重放攻击；角色权限控制（RBAC）保证数据隔离；患者对话记录加密存储；亲友仅可查看脱敏摘要信息 |
| **可用性** | 7×24 小时服务可用；AI 服务异常时降级为友好错误提示；Gunicorn 多 Worker 进程保障并发处理能力 |
| **可扩展性** | 前后端分离架构，支持独立扩展；AI 服务独立部署，可替换为其他 LLM；数据库层可从 SQLite 迁移至 PostgreSQL |
| **兼容性** | 患者端基于 Web 技术，支持主流浏览器（Chrome/Safari/Firefox）；响应式设计适配桌面端和移动端 |
| **隐私保护** | 支持用户注销账号并级联删除所有数据；亲友端不暴露完整对话内容；数据导出需管理员权限 |

### 3.2 总体设计

#### 3.2.1 架构设计

系统采用 **前后端分离 + AI 微服务** 的三层架构：

```
┌─────────────────────────────────────────────────────────────────────┐
│                      表现层 (Presentation Layer)                      │
│                                                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │
│  │   患者端       │  │  管理后台      │  │  紧急响应      │               │
│  │ React Native  │  │ React + Vite │  │ Vue 3 + Vite │               │
│  │ (Expo Web)    │  │ TypeScript   │  │ TypeScript   │               │
│  │ 端口: /       │  │ 端口: /admin │  │ 端口: /emerg │               │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘               │
│         │                  │                  │                       │
├─────────┼──────────────────┼──────────────────┼───────────────────────┤
│         │        Nginx 反向代理 (:80)          │                       │
│         │          路由分发 · 静态资源           │                       │
├─────────┼──────────────────┼──────────────────┼───────────────────────┤
│         ▼                  ▼                  ▼                       │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │                   业务逻辑层 (Business Layer)                 │     │
│  │                                                               │     │
│  │  ┌─────────────────────────────────────────────────────────┐ │     │
│  │  │            Django REST API (:8000)                       │ │     │
│  │  │  认证模块 · 情绪管理 · 对话管理 · 危机告警 · 康复计划      │ │     │
│  │  │  评估管理 · 患者管理 · 亲友支持 · 内容管理 · 数据导出      │ │     │
│  │  └────────────────────────┬────────────────────────────────┘ │     │
│  │                           │ HTTP                              │     │
│  │  ┌────────────────────────▼────────────────────────────────┐ │     │
│  │  │            Flask AI Service (:5000)                      │ │     │
│  │  │  Claude API 调用 · 流式响应 · 危机关键词检测 · TTS 合成   │ │     │
│  │  └─────────────────────────────────────────────────────────┘ │     │
│  └─────────────────────────────────────────────────────────────────┘     │
│                                                                       │
├───────────────────────────────────────────────────────────────────────┤
│                       数据持久层 (Data Layer)                          │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │                     SQLite Database                           │     │
│  │  User · MoodEntry · ChatMessage · CrisisAlert · Assessment  │     │
│  │  RecoveryTask · RecoveryBadge · Article · SupporterLink     │     │
│  └─────────────────────────────────────────────────────────────┘     │
└───────────────────────────────────────────────────────────────────────┘
```

**架构设计说明：**

1. **表现层**：三个独立的前端应用，分别面向不同用户群体。患者端采用 React Native (Expo) 实现跨平台能力，管理后台采用 React 实现丰富的数据管理交互，紧急响应系统采用 Vue 实现轻量级实时监控面板。三者共享同一套后端 API。

2. **Nginx 代理层**：Nginx 作为统一入口，根据 URL 路径将请求分发至不同的前端静态资源目录或后端 API 服务。SSE 流式接口配置了特殊的代理参数（关闭缓冲、延长超时）以支持长连接。

3. **业务逻辑层**：采用 Django + Flask 双服务架构。Django 负责核心业务逻辑（认证、CRUD、权限控制），Flask 负责 AI 相关的计算密集型任务（LLM 调用、TTS 合成）。两者通过 HTTP 内部通信，实现关注点分离。

4. **数据持久层**：使用 SQLite 作为关系型数据库，通过 Django ORM 进行数据访问。数据模型共 12 个实体，涵盖用户、情绪、对话、告警、评估、康复、文章等核心业务领域。

#### 3.2.2 流程设计

**核心业务流程 1：AI 对话与危机检测**

```
┌──────┐    POST /api/chat/     ┌──────────┐    /crisis_check    ┌──────────┐
│ 患者  │ ──send_message_stream─→│  Django   │ ──────────────────→│  Flask   │
│ 前端  │                        │  Backend  │                    │ AI Service│
└──┬───┘                        └──┬───┬───┘                    └──┬───┬───┘
   │                               │   │                           │   │
   │                               │   │  1. 存储用户消息            │   │
   │                               │   │  2. 发送文本给Flask检测      │   │
   │                               │   │←── 返回 {is_crisis, level} ─┘   │
   │                               │   │                                  │
   │                               │   │  3. 若 is_crisis=true            │
   │                               │   │     创建 CrisisAlert             │
   │                               │   │                                  │
   │                               │   │  4. 构建对话历史(最近20条)        │
   │                               │   │     调用 /respond_stream          │
   │                               │   │──────────────────────────────────→│
   │                               │   │                                   │
   │    SSE: data: {"delta":"你"}   │   │←──── SSE 流式返回 AI 回复 ────────┘
   │←──────────────────────────────┘   │
   │    SSE: data: {"delta":"好"}       │
   │←──────────────────────────────────┘
   │                               │
   │                               │  5. 存储完整 AI 回复
   │    SSE: data: {"done": true}  │
   │←──────────────────────────────┘
```

**核心业务流程 2：危机告警响应**

```
┌──────┐                    ┌──────────┐                ┌────────────┐
│ 患者  │  SOS / 危机检测触发  │  Django   │  每5秒轮询      │ 紧急响应系统  │
│ 前端  │ ─POST /crisis_alerts→│  Backend  │←──GET /active──│ emergency  │
└──────┘                    └──────────┘                └─────┬──────┘
                                                              │
                    ┌─────────────────────────────────────────┘
                    │
                    ▼
         ┌─────────────────┐     确认接收      ┌──────────────────┐
         │  活跃(active)    │ ──PATCH status──→ │ 处理中(acknowledged)│
         │  NEW 徽章闪烁    │  + 处理记录       │                    │
         │  >5min 超时高亮  │                  │                    │
         └─────────────────┘                  └────────┬─────────┘
                                                        │ 标记已解决
                                                        │ + 处理记录
                                                        ▼
                                               ┌──────────────────┐
                                               │  已解决(resolved)  │
                                               │  从活跃列表移除     │
                                               └──────────────────┘
```

**核心业务流程 3：康复计划与成就系统**

```
患者进入今日计划页面
    │
    ▼
GET /api/recovery/tasks
    │
    ├── 今日已有任务 → 返回已有任务列表
    │
    └── 今日无任务 → 自动生成:
        从6种模板中随机选取3个:
        ┌─────────────────────────────────────┐
        │ 情绪记录 │ 正念呼吸 │ 科普阅读       │
        │ 运动锻炼 │ 社交互动 │ 感恩日记       │
        └─────────────────────────────────────┘
    │
    ▼
用户完成任务 → POST /tasks/<id>/complete
    │
    ▼
后端检查并颁发徽章:
    ├── 首次类: first_mood / first_chat / first_assessment
    ├── 里程碑: tasks_10 / tasks_50
    ├── 连续打卡: streak_3 / streak_7 / streak_30
    └── 进步类: mood_improve
```

### 3.3 模块/子问题详细设计

#### 3.3.1 数据模型设计

系统共定义 12 个数据模型，核心实体关系如下：

```
User (用户)
 │
 ├──1:N──→ MoodEntry (情绪记录)
 ├──1:N──→ ChatMessage (对话消息)
 ├──1:N──→ CrisisAlert (危机告警)
 ├──1:N──→ AssessmentResult (评估结果)
 ├──1:N──→ RecoveryTask (康复任务)
 ├──1:N──→ RecoveryBadge (成就徽章)
 │
 ├──M:N──→ PatientAssignment ←──M:N── User(professional)
 ├──M:N──→ SupporterLink ←──M:N── User(supporter)
 │
 ├──1:N(sender)──→ EncouragementMessage ←──N:1(receiver)── User
 └──1:N(author)──→ Article (文章)
```

**核心模型字段设计：**

| 模型 | 关键字段 | 说明 |
|------|---------|------|
| **User** | id, username, email, phone, role(patient/professional/supporter/admin), created_at | 自定义用户模型，继承 AbstractUser，role 字段实现四角色区分 |
| **MoodEntry** | user(FK), mood_score(1-10), content(text), created_at | 情绪打分 + 文字记录，按时间倒序排列 |
| **ChatMessage** | user(FK), content, is_ai_response(bool), session_id(UUID), created_at | 用户消息和 AI 回复统一存储，session_id 分组为对话 |
| **CrisisAlert** | user(FK), level(low/medium/high/critical), status(active/acknowledged/resolved), description, handled_by(FK), processing_notes, acknowledged_at, resolved_at | 完整的危机告警生命周期管理 |
| **AssessmentResult** | user(FK), assessment_type(phq9/gad7), total_score, answers(JSON), severity | 标准化量表评估结果，answers 字段存储逐题得分 |
| **RecoveryTask** | user(FK), task_type(6种), title, description, is_completed, date, completed_at | 每日康复任务，按日期分组 |
| **RecoveryBadge** | user(FK), badge_type(9种), earned_at | 成就徽章，(user, badge_type) 唯一约束防止重复颁发 |
| **Article** | title, summary, content, url, category(7种), author(FK), is_published | 科普文章，url 字段支持外部链接 |

#### 3.3.2 API 接口设计

系统 API 采用 RESTful 风格，共包含 33+ 个端点，按功能模块分组：

| 模块 | 端点数 | 认证要求 | 核心端点 |
|------|--------|---------|---------|
| 认证 | 3 | 无 | POST /api/auth/login, /register, /token/refresh |
| 用户 | 2 | 需登录 | GET /api/users/me, DELETE /api/users/delete_account |
| 情绪记录 | 2 | 角色限制 | POST/GET /api/mood_entries |
| AI 对话 | 5 | 需登录 | POST /api/chat/send_message_stream (SSE) |
| 心理评估 | 2 | 角色限制 | POST/GET /api/assessments |
| 危机告警 | 5 | 部分公开 | POST /api/crisis_alerts, GET /active |
| 患者管理 | 3 | admin/professional | GET /api/patients/<id>/detail |
| 康复计划 | 4 | 需登录 | GET /api/recovery/tasks, POST /complete |
| 亲友支持 | 5 | 角色限制 | POST /api/supporter/link_patient |
| 文章管理 | 5 | 角色限制 | CRUD /api/articles |
| 管理员 | 7 | admin | GET /api/admin/users, /stats |
| 数据导出 | 3 | admin/professional | GET /api/admin/export/* (CSV) |
| TTS | 1 | 需登录 | POST /api/tts |

**权限控制矩阵：**

| 接口 | patient | supporter | professional | admin | 未登录 |
|------|---------|-----------|--------------|-------|--------|
| 创建情绪记录 | ✓ | ✗ | ✗ | ✗ | ✗ |
| 查看情绪记录 | 自己的 | ✗ | 分配患者的 | 全部 | ✗ |
| AI 对话 | ✓ | ✗ | ✗ | ✗ | ✗ |
| 创建危机告警 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 查看活跃告警 | ✗ | ✗ | ✓ | ✓ | ✓ |
| 患者详情 | ✗ | ✗ | 分配患者 | 全部 | ✗ |
| 患者状态摘要 | ✗ | 关联患者 | 分配患者 | 全部 | ✗ |
| 用户管理 | ✗ | ✗ | ✗ | ✓ | ✗ |

#### 3.3.3 前端模块设计

**患者端模块划分：**

| 模块 | 组件文件 | 核心状态 | 交互方式 |
|------|---------|---------|---------|
| 认证模块 | LoginScreen, AuthContext | token, userRole | 表单输入，JWT 存储 |
| 情绪记录 | MoodScreen | entries, trendRange, score | 表情选择器, 柱状图, 下拉刷新 |
| AI 对话 | ChatScreen | messages, sessions, isRecording | SSE 流式, 语音输入/播放 |
| 心理评估 | AssessmentScreen | screen(select/quiz/result/history), answers | 4 状态页面切换, 逐题作答 |
| 今日计划 | RecoveryPlanScreen | tasks, badges, stats | 任务卡片, 打卡交互 |
| 科普阅读 | ArticleScreen | articles, selectedCat, selectedArticle | 分类标签, 列表/详情切换 |
| 紧急求助 | EmergencyButton | visible, customContacts | 悬浮按钮, Modal, 通讯录 |
| 守护者模式 | SupporterHomeScreen, EncouragementScreen | links, patientStatus | 关联搜索, 消息发送 |

**管理后台模块划分：**

| 模块 | 页面文件 | 核心功能 |
|------|---------|---------|
| 患者管理 | DashboardPage, PatientDetailPage | 列表搜索分页, 详情全景+趋势切换 |
| 危机监控 | CrisisAlertPage, AlertHistoryPage | 5秒轮询, 筛选, 全景弹窗 |
| 数据分析 | StatsPage | 统计仪表盘, CSV 导出 |
| 内容管理 | ArticleManagementPage | 文章 CRUD, 外部链接支持 |
| 用户管理 | UserManagementPage, AssignmentPage | 角色管理, 分配关系 |
| 干预建议 | InterventionLibraryPage | 方案库检索 |

---

## 四、系统实现/实验验证

### 4.1 开发/实验环境

**开发环境：**

| 项目 | 配置 |
|------|------|
| 操作系统 | macOS Darwin 24.6.0 |
| Node.js | 18+ |
| Python | 3.9+ |
| 包管理器 | pnpm (前端), pip (后端) |
| 代码编辑器 | VS Code |

**生产环境：**

| 项目 | 配置 |
|------|------|
| 云服务器 | 阿里云 ECS |
| 操作系统 | Ubuntu 22.04 LTS |
| CPU | 2 vCPU |
| 内存 | 1.6 GB RAM |
| Web 服务器 | Nginx 1.18 |
| 应用服务器 | Gunicorn (2 workers × 2 services) |
| 数据库 | SQLite 3 |
| 进程管理 | systemd |
| 公网 IP | 47.239.219.238 |

**依赖版本：**

| 依赖 | 版本 | 用途 |
|------|------|------|
| Django | 4.2 | 后端框架 |
| Django REST Framework | 3.14+ | API 框架 |
| djangorestframework-simplejwt | 5.3+ | JWT 认证 |
| Flask | 3.0+ | AI 微服务 |
| edge-tts | 7.0+ | 文字转语音 |
| React | 19.0 | 管理后台 UI |
| React Native (Expo) | SDK 52 | 患者端 UI |
| Vue | 3.4 | 紧急响应 UI |
| axios | 1.7+ | HTTP 客户端 |
| Claude Haiku 4.5 | claude-haiku-4-5-20251001 | AI 大语言模型 |

### 4.2 核心模块实现

#### 4.2.1 AI 对话模块实现

**Flask AI 服务端关键代码（app.py）：**

```python
# AI 系统提示词 —— 定义 AI 助手的角色和行为准则
SYSTEM_PROMPT = """你是一个温暖的心理健康助手。请用以下方式回应用户：
1. 以温暖、共情、非评判的态度倾听
2. 引导用户探索和表达情绪
3. 不替代专业心理治疗或药物处方
4. 如识别到高风险信号，推荐危机热线 400-161-9995
5. 用中文回复，2-4句话，朋友式的亲切语气"""

# Claude API 流式调用
@app.route("/respond_stream", methods=["POST"])
def respond_stream():
    data = request.get_json()
    text = data.get("text", "")
    history = data.get("history", [])

    # 构建 Anthropic 消息格式
    messages = [{"role": m["role"], "content": m["content"]} for m in history]
    messages.append({"role": "user", "content": text})

    # 调用 Claude API (stream=True)
    resp = requests.post(AI_BASE_URL, headers=headers, json={
        "model": AI_MODEL,
        "max_tokens": 500,
        "system": SYSTEM_PROMPT,
        "messages": messages,
        "stream": True
    }, stream=True, timeout=60)

    # SSE 逐 token 返回
    def generate():
        for line in resp.iter_lines(decode_unicode=True):
            if line.startswith("data:"):
                chunk = json.loads(line[5:].strip())
                if chunk.get("type") == "content_block_delta":
                    delta = chunk["delta"].get("text", "")
                    yield f"data: {json.dumps({'delta': delta})}\n\n"
        yield "data: [DONE]\n\n"

    return Response(generate(), mimetype="text/event-stream")
```

**前端 SSE 流式接收（ChatScreen.tsx）核心逻辑：**

```typescript
// 发送消息并接收流式回复
const handleSend = async () => {
  const userMsg = { id: `user-${Date.now()}`, content: inputText,
                    is_ai_response: false };
  const streamId = `ai-stream-${Date.now()}`;
  const aiPlaceholder = { id: streamId, content: '', is_ai_response: true };

  setMessages(prev => [...prev, userMsg, aiPlaceholder]);

  await chatAPI.sendMessageStream(inputText, sessionId, {
    onDelta: (delta: string) => {
      // 实时拼接 AI 回复内容（打字机效果）
      setMessages(prev => prev.map(m =>
        m.id === streamId ? { ...m, content: m.content + delta } : m
      ));
    },
    onAiMsg: (msg) => {
      // 流式完成，替换为服务端持久化的消息 ID
      setMessages(prev => prev.map(m =>
        m.id === streamId ? { ...m, id: `ai-${msg.id}` } : m
      ));
    }
  });
};
```

#### 4.2.2 危机检测与告警模块实现

**关键词匹配检测（Flask app.py）：**

```python
CRISIS_KEYWORDS = [
    "想死", "自杀", "不想活", "结束生命", "跳楼", "割腕", "去死",
    "活不下去", "了结", "轻生", "死了算了", "没有意义", "不想活了",
    "服药自杀", "上吊", "跳河", "跳桥", "死掉", "寻死"
]

@app.route("/crisis_check", methods=["POST"])
def crisis_check():
    text = request.get_json().get("text", "")
    found = [kw for kw in CRISIS_KEYWORDS if kw in text]

    if len(found) >= 3:   risk_level = "high"
    elif len(found) == 2: risk_level = "medium"
    elif len(found) == 1: risk_level = "low"
    else:                 risk_level = "none"

    return jsonify({
        "is_crisis": len(found) > 0,
        "keywords_found": found,
        "risk_level": risk_level
    })
```

**Django 端对话中集成危机检测（views.py）：**

```python
def send_message_stream(request):
    content = request.data.get("content", "")
    user = request.user

    # 1. 存储用户消息
    user_msg = ChatMessage.objects.create(
        user=user, content=content, session_id=session_id)

    # 2. 危机检测
    crisis_resp = requests.post(f"{AI_URL}/crisis_check",
                                json={"text": content})
    crisis_data = crisis_resp.json()

    if crisis_data.get("is_crisis"):
        CrisisAlert.objects.create(
            user=user,
            level=crisis_data["risk_level"],
            description=f"AI检测到危机关键词: {crisis_data['keywords_found']}"
        )

    # 3. 构建历史上下文(最近20条)并调用AI
    history = build_chat_history(user, session_id, limit=20)
    ai_response = requests.post(f"{AI_URL}/respond_stream",
                                json={"text": content, "history": history},
                                stream=True)

    # 4. SSE 流式返回
    return StreamingHttpResponse(
        stream_ai_response(ai_response, user, session_id),
        content_type="text/event-stream"
    )
```

#### 4.2.3 心理评估模块实现

**PHQ-9 量表问题定义与评分（AssessmentScreen.tsx）：**

```typescript
const PHQ9_QUESTIONS = [
  '做事时提不起劲或没有兴趣',
  '感到心情低落、沮丧或绝望',
  '入睡困难、睡不安稳或睡眠过多',
  '感觉疲倦或没有活力',
  '食欲不振或吃太多',
  '觉得自己很糟糕——或觉得自己是个失败者',
  '注意力难以集中',
  '行动或说话速度变慢/坐立不安',
  '有不如死掉或用某种方式伤害自己的念头'
];

const OPTION_SCORES = [0, 1, 2, 3]; // 完全没有/有几天/超过一半/几乎每天

// 提交评估并自动判定严重度
const handleAnswer = (score: number) => {
  const newAnswers = [...answers, score];
  if (currentQ + 1 >= questions.length) {
    const totalScore = newAnswers.reduce((a, b) => a + b, 0);
    const severity = assessmentType === 'phq9'
      ? totalScore <= 4 ? '正常' : totalScore <= 9 ? '轻度'
        : totalScore <= 14 ? '中度' : totalScore <= 19 ? '中重度' : '重度'
      : totalScore <= 4 ? '正常' : totalScore <= 9 ? '轻度'
        : totalScore <= 14 ? '中度' : '重度';

    assessmentAPI.create({
      assessment_type: assessmentType,
      total_score: totalScore,
      answers: newAnswers,
      severity
    });
  }
};
```

#### 4.2.4 康复计划与成就系统实现

**每日任务自动生成（Django views.py）：**

```python
TASK_TEMPLATES = {
    "mood_log":    ("记录今天的心情", "花几分钟记录你此刻的情绪状态和想法"),
    "mindfulness": ("5分钟正念呼吸", "找一个安静的地方，专注于呼吸5分钟"),
    "reading":     ("阅读一篇心理健康文章", "在科普阅读中了解心理健康知识"),
    "exercise":    ("30分钟散步或运动", "适度的身体运动有助于改善情绪"),
    "social":      ("和一个人聊天", "与家人或朋友进行一次简短的交流"),
    "gratitude":   ("写下3件感恩的事", "记录今天让你感到感恩的三件小事"),
}

def _generate_daily_tasks(user, date):
    existing = RecoveryTask.objects.filter(user=user, date=date).count()
    if existing > 0:
        return  # 今天已有任务

    # 随机选取3种任务类型
    selected = random.sample(list(TASK_TEMPLATES.keys()), 3)
    for task_type in selected:
        title, description = TASK_TEMPLATES[task_type]
        RecoveryTask.objects.create(
            user=user, task_type=task_type, title=title,
            description=description, date=date
        )
```

**连续打卡检测与徽章颁发：**

```python
def _check_and_award_badges(user):
    # 连续打卡天数检测
    today = timezone.now().date()
    streak = 0
    check_date = today
    while True:
        completed = RecoveryTask.objects.filter(
            user=user, date=check_date, is_completed=True
        ).exists()
        if not completed:
            break
        streak += 1
        check_date -= timedelta(days=1)

    # 颁发对应徽章
    badge_map = {3: "streak_3", 7: "streak_7", 30: "streak_30"}
    for threshold, badge_type in badge_map.items():
        if streak >= threshold:
            RecoveryBadge.objects.get_or_create(
                user=user, badge_type=badge_type
            )
```

#### 4.2.5 语音交互模块实现

**语音识别（useSpeechRecognition.web.ts）：**

```typescript
export function useSpeechRecognition(onTranscript: (text: string) => void) {
  const SpeechRecognition = window.SpeechRecognition
                         || window.webkitSpeechRecognition;
  const isAvailable = !!SpeechRecognition;

  const start = () => {
    const recognition = new SpeechRecognition();
    recognition.lang = 'zh-CN';
    recognition.continuous = true;
    recognition.interimResults = true;  // 实时返回中间结果

    recognition.onresult = (event) => {
      let finalTranscript = '';
      let interimTranscript = '';
      for (let i = 0; i < event.results.length; i++) {
        const result = event.results[i];
        if (result.isFinal) finalTranscript += result[0].transcript;
        else interimTranscript += result[0].transcript;
      }
      onTranscript(finalTranscript + interimTranscript);
    };
    recognition.start();
  };

  return { isRecording, isAvailable, start, stop };
}
```

**TTS 语音播放（useAudioPlayer.web.ts）：**

```typescript
const play = async (id: string, text: string) => {
  stop();  // 停止当前播放
  setLoadingId(id);

  const response = await ttsAPI.speak(text);       // POST /api/tts
  const blob = new Blob([response.data], { type: 'audio/mpeg' });
  const url = URL.createObjectURL(blob);

  const audio = new Audio(url);
  audio.onended = () => {
    URL.revokeObjectURL(url);  // 释放内存
    setPlayingId(null);
  };

  setPlayingId(id);
  await audio.play();
};
```

### 4.3 测试/实验设计

#### 4.3.1 测试用例/实验方案

系统测试分为 **接口测试** 和 **端到端联调测试** 两部分：

**接口测试方案（共 33 个端点）：**

| 测试类别 | 测试内容 | 验证标准 |
|---------|---------|---------|
| 认证接口 | 注册、登录（4种角色）、Token 刷新 | 返回 200/201，包含有效 JWT |
| 情绪记录 | 创建情绪记录、查询列表、权限隔离 | 患者可创建(201)，权限外角色被拒(403) |
| AI 对话 | 同步对话、SSE 流式对话、会话管理 | AI 正确回复，SSE 格式正确，会话分组正确 |
| 心理评估 | 提交 PHQ-9/GAD-7、查询历史 | 评分正确，严重度判定一致 |
| 危机告警 | 创建告警、状态流转、统计查询 | 状态机转换正确，统计数据一致 |
| 康复计划 | 任务生成、完成打卡、徽章颁发 | 每日生成3任务，徽章不重复颁发 |
| 亲友支持 | 关联患者、发送鼓励、未读计数 | 关联关系建立正确，消息送达 |
| 数据导出 | CSV 导出（患者/情绪/告警） | 返回 200，Content-Type 为 text/csv |
| TTS 合成 | 文字转语音 | 返回 200，Content-Type 为 audio/mpeg |

**端到端联调测试方案：**

| 测试场景 | 涉及系统 | 测试步骤 |
|---------|---------|---------|
| 情绪记录联调 | 患者端 → 管理后台 | 患者创建情绪记录 → 管理后台查看患者详情验证记录存在 |
| AI 对话危机联调 | 患者端 → Flask → 紧急响应 | 患者发送含危机关键词的消息 → 验证告警自动创建 → 紧急响应系统展示告警 |
| 亲友鼓励联调 | 守护者端 → 患者端 | 守护者关联患者 → 发送鼓励 → 患者个人中心查看消息 |
| SOS 求助联调 | 患者端 → 紧急响应 → 管理后台 | 患者 SOS → 紧急响应系统收到告警 → 管理后台确认接收并解决 |

#### 4.3.2 测试/实验过程

**测试执行方法：** 使用 curl 命令行工具对生产环境（http://47.239.219.238）逐一调用所有 API 端点，验证 HTTP 状态码和响应数据格式。

**测试执行步骤：**

1. **预置测试数据**：创建 4 种角色的测试账号（patient1、doctor1、admin、supporter1）；
2. **认证测试**：依次登录 4 种角色，获取并保存 JWT Token；
3. **逐模块接口测试**：按模块顺序调用所有 33 个端点，验证响应状态码和数据；
4. **联调测试**：按场景执行端到端数据流验证；
5. **前端页面测试**：验证 3 个前端应用的页面可访问性和静态资源加载；
6. **服务状态验证**：检查 Nginx、Django、Flask 三个服务进程状态。

---

## 五、结果分析与讨论

### 5.1 功能测试结果

#### 5.1.1 接口测试结果

对生产环境 33 个 API 端点进行全面测试，结果如下：

| 测试类别 | 端点数 | 通过数 | 通过率 | 说明 |
|---------|--------|--------|--------|------|
| 认证接口 | 3 | 3 | 100% | 登录、注册、Token 刷新全部正常 |
| 用户接口 | 2 | 2 | 100% | 4 种角色均可获取个人信息 |
| 情绪记录 | 2 | 2 | 100% | 创建返回 201，列表返回 200 |
| AI 对话 | 5 | 5 | 100% | 同步/流式对话均正常回复 |
| 心理评估 | 2 | 2 | 100% | 评估提交和查询正常 |
| 危机告警 | 5 | 5 | 100% | 创建、查询、状态更新、统计正常 |
| 患者管理 | 3 | 3 | 100% | 列表、详情、状态摘要正常 |
| 康复计划 | 4 | 4 | 100% | 任务生成、完成、徽章、统计正常 |
| 亲友支持 | 5 | 5 | 100% | 关联、鼓励消息、未读计数正常 |
| 文章管理 | 5 | 5 | 100% | CRUD + URL 字段正常 |
| 管理员 | 7 | 7 | 100% | 用户管理、统计、分配正常 |
| 数据导出 | 3 | 3 | 100% | CSV 导出正常 |
| TTS 合成 | 1 | 1 | 100% | 音频生成正常(9216 bytes) |
| **合计** | **33+** | **33+** | **100%** | **全部通过** |

#### 5.1.2 前端页面与服务状态

| 检查项 | 状态 |
|--------|------|
| 患者端页面 (/) | 200 OK，JS/CSS 加载正常 |
| 管理后台 (/admin-web/) | 200 OK，JS/CSS 加载正常 |
| /admin-web 自动重定向 | 301 → /admin-web/ 正常 |
| 紧急响应 (/emergency/) | 200 OK，JS/CSS 加载正常 |
| Nginx 服务 | active (running) |
| Django Backend 服务 | active (running) |
| Flask AI 服务 | active (running) |

#### 5.1.3 端到端联调结果

| 测试场景 | 结果 |
|---------|------|
| 患者创建情绪记录 → 管理员查看详情 | 通过。创建记录后管理后台可立即查询到，平均情绪分正确计算 |
| AI 普通对话 | 通过。AI 以中文温暖回复，流式传输正常 |
| 守护者查看关联患者状态 | 通过。返回脱敏的状态摘要信息 |
| 创建含 URL 文章 → 患者端查看 | 通过。url 字段正确存储和返回 |
| TTS 语音合成 | 通过。返回有效 MP3 音频 |

### 5.2 系统特色与创新点

1. **AI + 人工双重安全保障**：系统将关键词自动检测与人工专业响应相结合，实现了从危机识别到干预处理的完整闭环。AI 对话中实时进行危机检测，一旦触发即自动创建告警并推送至紧急响应系统，平均检测延迟 <1 秒。

2. **多端协同的全角色覆盖**：系统设计了患者、守护者（亲友）、专业人员、管理员四种角色，分别通过不同的前端界面和权限策略提供针对性功能，形成了"患者自助 → AI 辅助 → 亲友支持 → 专业干预"的分层服务体系。

3. **游戏化康复激励机制**：通过每日任务自动生成、连续打卡统计、9 种成就徽章，将康复过程游戏化，提高患者的长期参与度和依从性。

4. **隐私保护设计**：亲友只能查看脱敏的状态摘要，无法访问完整对话记录；用户可随时注销账号并级联删除所有数据；数据导出需管理员权限审批。

5. **轻量化部署方案**：整套系统（3 个前端 + 2 个后端服务 + 数据库）运行在一台 2 vCPU / 1.6GB RAM 的低配云服务器上，证明了该方案的低成本可行性。

### 5.3 不足与展望

1. **危机检测精度**：当前基于关键词匹配的检测方法存在一定局限性，无法识别隐晦表达（如"我想离开这个世界"中不含任何预设关键词）。未来可引入 NLP 语义分析或微调 LLM 分类器以提升召回率。

2. **评估量表局限**：目前仅支持 PHQ-9 和 GAD-7 两种量表。后续可扩展支持 BDI-II（贝克抑郁量表）、HAMD（汉密尔顿抑郁量表）等更多标准化工具。

3. **数据安全增强**：当前数据库为 SQLite 单文件存储，生产环境应迁移至 PostgreSQL 并启用传输层加密（TLS）和静态数据加密。

4. **AI 能力升级**：当前 AI 仅进行单轮共情式回应。未来可引入多轮 CBT 结构化对话策略，实现自动化的认知重建引导。

5. **移动端原生化**：当前患者端以 Web 形式部署，未来可利用 Expo 的原生构建能力发布 iOS/Android 应用，获得推送通知、后台运行等原生能力。

---

## 附录 A：系统线上地址

| 系统 | URL |
|------|-----|
| 患者端（天使/守护者） | http://47.239.219.238/ |
| 管理后台 | http://47.239.219.238/admin-web/ |
| 紧急响应中心 | http://47.239.219.238/emergency/ |

## 附录 B：数据库迁移记录

| 迁移文件 | 说明 |
|----------|------|
| 0001_initial | 初始模型（User, MoodEntry, ChatMessage, CrisisAlert） |
| 0002_alter_user_phone | phone 字段唯一约束 |
| 0003_alter_user_role | 增加 supporter 角色 |
| 0004_crisis_alert_workflow | 告警状态流转字段 |
| 0005_add_processing_notes | 告警处理记录字段 |
| 0006_add_chat_feedback | ChatFeedback 模型 |
| 0007_add_assessment_result | AssessmentResult 模型 |
| 0008_add_supporter_and_encouragement | SupporterLink, EncouragementMessage 模型 |
| 0009_add_recovery_plan | RecoveryTask, RecoveryBadge 模型 |
| 0010_add_article | Article 模型 |
| 0011_add_article_url | Article 增加 url 字段 |
| 0012_add_composite_indexes_and_validators | 5 个复合索引 + mood_score 验证器 |
| 0013_optimize_constraints_indexes_and_fields | 架构评审优化：email NULLABLE、CrisisAlert updated_at、2 个新索引、UniqueConstraint 升级 |

## 附录 C：本地开发指南

```bash
# 后端
cd backend && python -m venv venv && source venv/bin/activate
pip install -r requirements.txt && python manage.py migrate
python manage.py runserver 8000

# AI 服务
cd ai_service && python -m venv venv && source venv/bin/activate
pip install -r requirements.txt && python app.py

# 患者端
cd patient-app && pnpm install && pnpm start          # :8081

# 管理后台
cd admin-web && pnpm install && pnpm dev               # :5173

# 紧急响应
cd emergency-web && pnpm install && pnpm dev           # :5174
```
