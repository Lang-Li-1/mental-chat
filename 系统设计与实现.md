# 系统设计与实现

## 1. 系统目标

本系统实现一个基于生成式 AI 的情绪缓解对话与危机预警平台。系统面向患者、守护者、专业人员和管理员四类角色，提供情绪记录、AI 对话、PHQ-9/GAD-7 自评、SOS 求助、危机告警、康复任务、科普文章和后台处置能力。

系统定位为轻量化心理支持和风险预警工具，不进行医学诊断，不替代专业心理治疗。

## 2. 总体架构

系统采用“多端前端 + Django 后端 + Flask AI 微服务”的架构。

```text
patient-app / admin-web / emergency-web
                  |
                  | REST / SSE / WebSocket
                  v
          Django + DRF + Channels
                  |
                  | HTTP
                  v
          Flask AI Service
                  |
                  v
       DashScope/Qwen / Edge-TTS
```

### 2.1 前端

| 端 | 技术 | 说明 |
| --- | --- | --- |
| `patient-app` | Expo、React Native、React Navigation | 患者端和守护者视角，根据 `role` 切换导航 |
| `admin-web` | React、Vite | 专业人员和管理员后台 |
| `emergency-web` | Vue 3、Vite | 紧急响应看板 |

### 2.2 后端

- Django / Django REST Framework：业务 API、权限控制、序列化。
- SimpleJWT：登录和 JWT 鉴权。
- Django Channels：告警 WebSocket 和患者-守护者私聊 WebSocket。
- Celery：异步危机检测和徽章计算；本地 Debug 场景下部分逻辑有同步兜底。
- SQLite / PostgreSQL：本地默认 SQLite，可通过环境变量切换 PostgreSQL。

### 2.3 AI 微服务

- Flask：提供 AI 服务 API。
- DashScope/Qwen OpenAI-compatible API：生成 AI 对话回复。
- SSE：`/respond_stream` 流式输出回复片段。
- 关键词规则：`/crisis_check` 检测危机关键词并返回风险级别。
- Edge-TTS：`/tts` 输出 MP3 音频。

## 3. 用户角色与权限

| 角色 | 权限 |
| --- | --- |
| 患者 `patient` | 情绪记录、量表评估、AI 对话、SOS、康复任务、文章阅读、守护者私聊 |
| 守护者 `supporter` | 绑定患者、查看脱敏状态摘要、发送鼓励、私聊 |
| 专业人员 `professional` | 查看分配患者、处理告警、查看患者详情、维护文章 |
| 管理员 `admin` | 用户管理、分配管理、统计导出、文章管理、全部后台能力 |

## 4. 功能设计与实现

### 4.1 情绪记录模块

实现位置：

- 前端：`patient-app/src/screens/MoodScreen.tsx`
- 后端：`MoodEntry`、`MoodEntryListCreateView`
- API：`GET/POST /api/mood_entries`

已实现能力：

- 患者创建 1-10 分情绪记录。
- 历史记录列表。
- 前端按今日、本月、本年、全部筛选并展示趋势柱状图。
- 前端情绪日历展示每日记录。
- 后端按角色限制访问：患者看自己，专业人员看分配患者，管理员可按参数查看。

说明：趋势聚合主要在前端完成；后端返回记录列表。

### 4.2 量表评估模块

实现位置：

- 前端：`patient-app/src/screens/AssessmentScreen.tsx`，由 `MoodScreen` 内的“量表评估”模块进入。
- 后端：`AssessmentResult`、`AssessmentResultListCreateView`
- API：`GET/POST /api/assessments`

已实现能力：

- PHQ-9 抑郁量表：9 题，0-27 分。
- GAD-7 焦虑量表：7 题，0-21 分。
- 前端逐题作答、计算总分和严重度。
- 保存逐题分数、总分、严重度和时间。
- 历史评估记录查看。
- 专业人员和管理员可在患者详情中查看患者评估结果。

### 4.3 AI 对话模块

实现位置：

- 前端：`patient-app/src/screens/ChatScreen.tsx`
- 后端：`ChatMessage`、`send_message`、`send_message_stream`
- AI：`ai_service/app.py`
- API：`POST /api/chat/send_message`、`POST /api/chat/send_message_stream`

流程：

1. 患者发送消息。
2. Django 保存用户消息。
3. 触发危机检测。
4. Django 构造历史上下文并调用 AI 微服务。
5. 同步接口一次性返回，流式接口通过 SSE 逐片返回。
6. AI 回复保存为 `ChatMessage`。

已实现能力：

- 文本对话。
- SSE 流式回复。
- 会话列表和历史消息。
- AI 消息点赞/点踩反馈。
- 语音识别输入和 TTS 播放。

### 4.4 危机检测与告警模块

实现位置：

- AI 检测：`ai_service/app.py` 的 `/crisis_check`
- 告警模型：`CrisisAlert`
- 后端 API：`/api/crisis_alerts`、`/api/crisis_alerts/active`、`/api/crisis_alerts/<id>/status`
- 推送：`backend/api/signals.py`、`backend/api/consumers.py`

当前检测方式：

- 使用危机关键词列表进行文本匹配。
- 命中 1 个关键词：`low`。
- 命中 2 个关键词：`medium`。
- 命中 3 个及以上：`high`。
- 模型和前端保留 `critical` 等级，但当前 AI 规则不会自动返回 `critical`。

告警处置：

- 患者主动 SOS 或 AI 对话检测命中后创建告警。
- 创建告警时后端会校验用户绑定关系：患者只能为自己创建，专业人员只能为已分配患者创建，管理员必须指定患者。
- 告警创建或状态更新时，通过 Django signal 广播到 WebSocket 分组。
- 管理后台和紧急响应端接收实时更新，并通过轮询兜底。
- 专业人员/管理员可确认、解决并填写备注。

### 4.5 SOS 求助模块

实现位置：`patient-app/src/components/EmergencyButton.tsx`

已实现能力：

- AI 对话页右下角显示 SOS。
- 点击后创建高风险危机告警，后端强制绑定当前登录患者，避免伪造其他用户告警。
- 展示内置热线，如心理援助热线、生命热线、120、110。
- 支持自定义联系人和导入联系人能力。
- 调用 `tel:` 进行拨号。

### 4.6 守护者支持模块

实现位置：

- 前端：`SupporterHomeScreen.tsx`、`EncouragementScreen.tsx`
- 后端：`SupporterLink`、`EncouragementMessage`
- WebSocket：`/ws/chat/<peer_id>/?token=<jwt>`

已实现能力：

- 守护者通过患者用户名或手机号绑定患者。
- 守护者查看患者脱敏状态摘要。
- 守护者发送鼓励消息。
- 患者和守护者双向私聊。
- WebSocket 主链路，HTTP 兜底。

### 4.7 康复计划模块

实现位置：

- 前端：`RecoveryPlanScreen.tsx`
- 后端：`RecoveryTask`、`RecoveryBadge`
- API：`/api/recovery/tasks`、`/api/recovery/tasks/<id>/complete`、`/api/recovery/badges`、`/api/recovery/stats`

已实现能力：

- 系统自动生成每日任务。
- 患者完成任务并打卡。
- 统计总任务数、完成数、连续天数和徽章数。
- 根据首次记录、首次对话、首次评估、任务完成数量等发放徽章。


### 4.8 科普文章模块

实现位置：

- 患者端：`ArticleScreen.tsx`
- 管理端：`ArticleManagementPage.tsx`
- 后端：`Article`
- API：`/api/articles`、`/api/articles/<id>`、`/api/articles/parse_url`

已实现能力：

- 患者端按分类浏览文章。
- 文章详情展示。
- 外部 URL 文章跳转。
- 管理端新增、编辑、删除、发布/草稿。
- 管理端解析外部文章 URL，自动填充标题、摘要或正文。


### 4.9 后台管理模块

实现位置：`admin-web/src/pages/*`

已实现页面：

- `DashboardPage`：患者列表。
- `PatientDetailPage`：患者详情、情绪、告警、评估。
- `CrisisAlertPage`：活跃告警处置。
- `AlertHistoryPage`：告警历史。
- `StatsPage`：统计和 CSV 导出。
- `UserManagementPage`：用户管理。
- `AssignmentPage`：患者-专业人员分配。
- `ArticleManagementPage`：文章管理。
- `InterventionLibraryPage`：前端静态干预建议库。

### 4.10 紧急响应看板

实现位置：`emergency-web/src/components/CrisisDashboard.vue`

已实现能力：

- 拉取活跃告警。
- WebSocket 订阅 `/ws/alerts/?token=<jwt>`，连接时校验 `admin` / `professional` 角色。
- 30 秒轮询兜底。
- 风险级别统计。
- 新告警高亮。
- 告警确认和解决。

## 5. API 设计

### 5.1 主要患者 API

| 模块 | API |
| --- | --- |
| 情绪记录 | `GET/POST /api/mood_entries` |
| 量表评估 | `GET/POST /api/assessments` |
| AI 对话 | `POST /api/chat/send_message`、`POST /api/chat/send_message_stream` |
| 会话历史 | `GET /api/chat/sessions`、`GET /api/chat/sessions/<sid>/messages` |
| 危机告警 | `POST /api/crisis_alerts`，患者只能为自己创建，专业人员只能为已分配患者创建，管理员必须指定患者 |
| 康复任务 | `GET /api/recovery/tasks`、`POST /api/recovery/tasks/<id>/complete` |
| 文章 | `GET /api/articles`、`GET /api/articles/<id>` |
| TTS | `POST /api/tts` |

### 5.2 主要后台 API

| 模块 | API |
| --- | --- |
| 活跃告警 | `GET /api/crisis_alerts/active` |
| 告警状态 | `PATCH /api/crisis_alerts/<id>/status` |
| 告警统计 | `GET /api/crisis_alerts/stats` |
| 患者列表 | `GET /api/patients` |
| 患者详情 | `GET /api/patients/<id>/detail` |
| 用户管理 | `GET/POST /api/admin/users`、`PATCH /api/admin/users/<id>` |
| 分配管理 | `GET/POST /api/admin/assignments`、`DELETE /api/admin/assignments/<id>` |
| 数据统计 | `GET /api/admin/stats` |
| CSV 导出 | `/api/admin/export/patients`、`/api/admin/export/mood_entries`、`/api/admin/export/crisis_alerts` |
| 文章管理 | `POST/PUT/DELETE /api/articles` |

### 5.3 WebSocket

| 路径 | 说明 |
| --- | --- |
| `/ws/alerts/?token=<jwt>` | 危机告警实时推送，仅 `admin` / `professional` 可订阅 |
| `/ws/chat/<peer_id>/?token=<jwt>` | 患者-守护者双向聊天 |

## 6. 数据库设计

核心模型：

- `User`
- `MoodEntry`
- `ChatMessage`
- `ChatFeedback`
- `CrisisAlert`
- `AssessmentResult`
- `PatientAssignment`
- `SupporterLink`
- `EncouragementMessage`
- `RecoveryTask`
- `RecoveryBadge`
- `Article`

详见 `数据库设计.md`。

## 7. 部署设计

当前仓库提供：

- `ops/docker/docker-compose.yml`
- `ops/docker/Dockerfile.backend`
- `ops/docker/Dockerfile.ai_service`
- `ops/nginx/mental-chat.conf`
- `deploy*.sh`

典型部署方式：

1. 构建前端静态资源。
2. Nginx 托管前端并反向代理 API、SSE 和 WebSocket。
3. Django 使用 ASGI 方式运行以支持 Channels。
4. Flask AI 微服务独立运行。
5. 可选使用 PostgreSQL 和 Redis。

## 8. 测试与验证

当前可执行的本地验证包括：

`ash
pnpm -C patient-app exec tsc --noEmit
cd backend && DJANGO_DEBUG=true python manage.py check
cd backend && DJANGO_DEBUG=true python manage.py test
`
