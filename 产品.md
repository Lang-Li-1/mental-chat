# 心理守护产品最终形态
## 1. 核心能力

### 1.1 发现能力：多模态智能预警
- **语义理解风险模型**：基于深度学习的四分类模型（无/低/中/高风险），融合对话上下文、历史情绪、用户画像，准确率 > 95%。
- **语音情绪识别**：实时分析用户语音语速、音调、停顿，输出情绪状态（平静/悲伤/愤怒/恐惧），作为风险辅助信号。
- **时序风险预测**：基于用户历史数据（情绪、对话、生理）训练LSTM模型，提前7天预测高风险用户，实现主动干预。
- **风险趋势周报**：每周一向专业人员推送负责患者的情绪趋势摘要，高风险患者高亮提示

### 1.2 响应能力：智能闭环处置
- **告警详情全景**：应急人员点击告警可查看患者脱敏信息、最近情绪记录、相关对话摘要、历史风险等级，辅助精准干预。
- **强制处理记录**：确认告警时必须填写处理记录（文字），状态流转（确认→处理中→解决）全程留痕，支持后续审计。
- **智能升级机制**：告警5分钟未确认自动通过短信/语音通知值班组长，10分钟未处理升级至更高级别。
- **专业人员移动端**：基于H5的移动应用，支持患者列表、情绪趋势、实时告警推送（WebSocket/极光），响应不再受限于PC。
- **智能分流引擎**：根据风险等级、专业人员负荷、用户偏好，自动将新告警分配给最合适的专业人员，提升响应效率。

### 1.3 支持能力：个性化康复生态
- **AI个性化对话**：
  - 记忆用户病史、偏好（称呼、回复风格），支持多轮上下文。
  - 用户可点赞/点踩，反馈数据用于模型微调。
  - 根据风险等级动态调整回复策略（高风险时更强调安抚和求助引导）。
- **康复计划系统**：
  - 每日生成个性化任务（情绪记录、正念练习、科普阅读），完成获得成就徽章。
  - 基于用户情绪和行为推荐科普内容，引导主动成长。
- **亲友互动支持**：
  - 天使（患者）和守护者（亲友）双向实时聊天，主链路走 WebSocket（`/ws/chat/<peer_id>/?token=`），断网回落到 HTTP；进入会话页时把对方未读消息自动标已读。
  - 支持预设暖心消息模板（守护者侧快捷发送）+ 自定义文字。
  - 支持者指南：图文科普如何识别抑郁、有效沟通、自我照顾。
- **家属互助社区**：
  - 专家引导的匿名社区，家属可发帖、评论、参与线上讲座。
  - 社区内容经审核，确保安全和专业性。
- **AI辅助评估**：
  - 集成PHQ-9、GAD-7等标准化量表，自动计算并给出解读（非诊断），引导就医。
- **危机后随访**：
  - 危机解决后24小时自动发送关怀消息，邀请填写简短问卷（如“现在感觉如何？”），防止复发。

---

## 2. 各端最终功能

### 2.1 患者端
- **情绪记录**：表情+分数+文字，历史趋势图，情绪日历。
- **AI对话**：个性化陪伴，语音输入/播放，支持多模态输入（文字/语音），对话历史管理。
- **康复计划**：每日任务、成就徽章、科普推荐。
- **亲友关怀**：接收亲友鼓励消息，可设置是否允许接收。
- **自我评估**：定期量表测评，生成心理健康报告。
- **紧急求助**：悬浮SOS按钮，一键触发告警，内置危机热线。
- **个人中心**：用户信息、数据导出、账号删除、隐私设置。

### 2.2 亲友端（删除单独的系统，集成到患者端，根据注册时选择的角色判断是患者还是亲友的功能）
- **患者状态总览**：脱敏显示情绪趋势、风险等级、最后活跃时间。
- **发送鼓励**：从预设模板选择或自定义消息，发送给患者。
- **支持者指南**：科普文章、视频，学习如何有效支持。
- **个人中心**：绑定患者关系、通知设置。

### 2.3 专业人员端（Web后台 + 移动H5）
- **患者列表**：分页搜索，高风险患者置顶，支持标签筛选。
- **患者详情**：情绪时间线、对话摘要、危机告警历史、风险趋势图。
- **危机告警管理**：实时告警看板，支持确认、处理、填写记录。
- **干预建议库**：内置标准干预话术和行动指南，可一键复制。
- **移动端**：告警推送、快速查看患者状态。

### 2.4 应急响应系统（Web，取消单独一个系统，集成进管理后台，管理者和医生都能看）
- **实时仪表盘**：展示所有活跃告警，按等级颜色编码，支持详情查看。
- **告警处理**：确认、填写处理记录、解决，状态实时同步。
- **历史查询**：按时间、等级、状态筛选历史告警，支持导出报表。
- **升级监控**：超时未处理告警高亮显示。

### 2.5 管理后台（Admin）
- **登录与注册**：管理员登录；登录页支持「我是医生，我要创建账号」入口，医生可自助注册（角色强制 professional），无需邀请码。
- **用户管理**：管理所有角色用户；右上角"+ 新增管理员/医生"按钮可手动创建管理员/医生账号；支持禁用/启用、修改角色。
- **分配管理**：患者-专业人员分配，支持批量操作。
- **机构管理**：企业/学校客户管理，多部门、权限分级。
- **数据报表**：全局统计（用户活跃、告警趋势、任务完成率），支持导出。
- **内容管理**：科普文章、支持者指南、干预建议库的增删改查。
- **值班配置**：设置应急值班表及联系方式。

---

## 3. 技术架构

### 3.1 总体架构
```
┌─────────────────────────────────────────────────────────────┐
│                       客户端层                               │
│  React Native (患者/亲友) · React (专业后台&应急) │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      API 网关 (Kong/Nginx)                   │
│                路由 · 限流 · 认证 · 日志                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     业务服务 (Django)                         │
│  用户 · 情绪 · 对话 · 告警 · 分配 · 机构 · 订阅 · 报表          │
│   Celery · Redis · PostgreSQL · 消息队列 (RabbitMQ)          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     AI 微服务集群 (Flask/FastAPI)             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │危机检测服务│ │对话生成服务│ │语音情绪识别│ │TTS服务   │      │
│  │(BERT/ONNX)│ │(Claude/自研)│ │(emotion2vec)│ │(edge-tts)│      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  模型热加载 · 推理加速 (TensorRT) · 特征存储 (Redis)          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据平台                                │
│  数据湖 (Delta Lake) · 数据仓库 · 流处理 (Flink)             │
│  模型训练 (PyTorch/TensorFlow) · 特征平台                    │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 关键组件
- **实时通信**：WebSocket（Django Channels）用于告警推送、对话实时更新。
- **异步任务**：Celery 处理邮件发送、数据导出、模型推理等耗时任务。
- **可观测性**：
  - 指标：Prometheus + Grafana（QPS、延迟、错误率）
  - 日志：Filebeat + Elasticsearch + Kibana
  - 链路追踪：Jaeger
  - 告警：Alertmanager → 钉钉/企微
- **安全与合规**：
  - 全站 HTTPS，JWT 使用 RS256
  - 数据库敏感字段加密（AES-256）
  - 数据导出/删除接口，符合 GDPR
  - 等保三级认证，定期渗透测试
  - 数据脱敏：对外 API 返回匿名化数据

### 3.3 数据能力
- **数据湖**：存储全量原始数据（情绪、对话、生理、埋点），用于模型训练和分析。
- **特征平台**：统一管理用户画像、风险特征，供各模型调用。
- **模型生命周期**：支持模型版本管理、A/B测试、在线学习。
- **开放 API**：对外提供脱敏数据接口，需申请审批，用于科研和合作伙伴。

---

## 4. 最终形态的核心理念

- **从被动到主动**：不再等待用户求助，而是通过预测模型主动发现潜在危机。
- **从单点到生态**：患者不再是孤立个体，而是被 AI、亲友、专业人员、社区共同守护。
- **从通用到个性化**：每一次对话、每一个任务都根据用户独特情况量身定制。