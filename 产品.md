# 产品说明

## 1. 产品定位

本系统是一个基于生成式 AI 的情绪缓解对话与危机预警平台，面向患者、守护者、专业人员和管理员四类角色，提供轻量化的心理支持、风险识别和后台处置闭环。

系统不提供医学诊断，也不替代专业心理治疗。AI 回复用于情绪支持和引导，危机检测用于辅助预警。

## 2. 用户角色

| 角色 | 端 | 核心价值 |
| --- | --- | --- |
| 患者 | `patient-app` 患者视角 | 记录情绪、AI 倾诉、量表自评、康复任务、SOS 求助、阅读科普、联系守护者 |
| 守护者 | `patient-app` 守护者视角 | 绑定患者、查看脱敏状态摘要、鼓励和私聊 |
| 专业人员 | `admin-web`、`emergency-web` | 查看分配患者、处理危机告警、查看患者详情、维护文章 |
| 管理员 | `admin-web` | 用户管理、分配管理、统计、内容管理 |


## 3. 已实现的产品功能

### 3.1 患者自助支持

- 情绪记录：1-10 分评分、文字记录、历史记录。
- 趋势展示：按今日、本月、本年、全部查看趋势柱状图；月历展示每日情绪点。
- 量表评估：在情绪记录 Tab 内进入评估模块，支持 PHQ-9 和 GAD-7，提供自动计分、严重度展示和历史记录。
- AI 对话：文本输入、语音转写、SSE 流式回复、会话历史、AI 消息反馈、TTS 播放。
- SOS 求助：AI 对话页提供 SOS 按钮，创建高风险告警，并提供热线和自定义联系人拨打入口。
- 康复计划：系统自动生成每日任务，支持打卡、统计和徽章。
- 科普阅读：按分类浏览文章，支持文章详情和外部链接跳转。
- 守护者互动：查看已绑定守护者，与守护者双向私聊。

### 3.2 守护者支持

- 通过患者用户名或手机号绑定患者。
- 查看患者脱敏状态摘要，包括近期情绪、聊天数量、活跃告警数等。
- 发送鼓励消息。
- 与患者实时私聊，WebSocket 为主，HTTP 为兜底。

### 3.3 专业人员和管理员后台

- 患者列表和搜索。
- 患者详情：情绪记录、评估记录、危机告警、近期会话摘要。
- 危机处置：查看活跃告警、确认、解决、填写备注。
- 告警历史和统计。
- WebSocket 实时告警，轮询兜底。
- 用户管理：管理员创建和管理用户。
- 分配管理：管理员维护患者与专业人员关系。
- 内容管理：文章新增、编辑、删除、发布/草稿、外部 URL 解析。
- 干预建议库：前端静态干预方案，可按风险等级、分类和关键词筛选。
- CSV 导出：患者、情绪记录、危机告警。

### 3.4 紧急响应看板

- 面向值守人员展示活跃告警。
- 按风险等级排序和统计。
- 支持确认和解决。
- WebSocket 实时刷新，30 秒轮询兜底。

## 4. 关键流程

### 4.1 AI 对话与危机预警

1. 患者发送文本或语音转写后的消息。
2. Django 保存用户消息。
3. 系统调用 Flask `/crisis_check` 做关键词检测。
4. 命中危机关键词时创建 `CrisisAlert`。
5. Django 调用 Flask `/respond_stream` 获取 AI 流式回复。
6. 前端边接收边渲染 AI 回复。
7. 新告警通过 WebSocket 推送到后台和应急看板。

当前危机检测是关键词匹配和命中数量分级：1 个关键词为 `low`，2 个为 `medium`，3 个及以上为 `high`。模型中保留 `critical` 等级，但 AI 检测当前不会自动生成 `critical`。

### 4.2 情绪记录与量表评估

1. 患者在情绪记录页提交评分和文字记录。
2. 后端保存 `MoodEntry`。
3. 前端基于历史记录计算和展示趋势图、日历。
4. 患者点击情绪记录页中的“量表评估”模块。
5. 完成 PHQ-9 或 GAD-7 后，前端计算总分和严重度，并提交 `AssessmentResult`。
6. 历史评估记录可在同一模块中查看。

### 4.3 守护者支持

1. 守护者登录 `patient-app` 守护者视角。
2. 通过患者用户名或手机号绑定患者。
3. 查看患者脱敏状态摘要。
4. 通过鼓励消息和私聊提供社会支持。

### 4.4 告警处置

1. 告警创建后通过 Django signal 广播到 WebSocket 分组。
2. 管理后台和紧急响应端使用带 JWT 的 WebSocket 连接接收推送，仅管理员和专业人员可订阅；网络不稳定时通过轮询刷新。
3. 专业人员确认告警，填写处理备注。
4. 告警可被标记为已解决，记录处理人和时间。


## 6. 技术实现概览

| 模块 | 技术 |
| --- | --- |
| 患者端/守护者端 | Expo、React Native、React Native Web、React Navigation |
| 管理后台 | React、Vite、Axios |
| 紧急响应端 | Vue 3、Vite |
| 后端 | Django、Django REST Framework、SimpleJWT、Channels、Celery |
| AI 服务 | Flask、DashScope/Qwen OpenAI-compatible API、Edge-TTS |
| 数据库 | 默认 SQLite，可通过环境变量切换 PostgreSQL |
| 实时通信 | WebSocket：告警推送需校验 `admin` / `professional` JWT，患者-守护者私聊需校验绑定关系 |
| 流式对话 | SSE：Django 代理 Flask 流式响应 |
